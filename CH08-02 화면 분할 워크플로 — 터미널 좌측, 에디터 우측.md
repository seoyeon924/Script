---
tags: #CH08 #안티그래비티 #화면분할 #워크플로 #생산성
time: 15분
chapter: CH08
status: ✅ 대본완성
---

# CH08-02 화면 분할 워크플로 — 터미널 좌측, 에디터 우측

## 📌 클립 정보
- **예상시간:** 15분
- **유형:** 화면 녹화
- **준비물:** 안티그래비티 + 클로드 코드 연결 완료

---

## 학습 목표 선언 (30초)

이 클립을 마치면 두 가지를 할 수 있습니다.

첫 번째, 안티그래비티에서 터미널과 에디터를 효율적으로 분할 배치할 수 있습니다.
두 번째, 이 레이아웃에서 클로드 코드 작업을 모니터링하면서 토큰을 절약하는 방법을 설명할 수 있습니다.

---

## 왜 화면 분할이 중요한가 (2분)

클로드 코드만 사용할 때는 파일 내용을 확인하려면 클로드에게 물어봐야 합니다.

"test.py 내용 보여줘." → 토큰 사용

안티그래비티 에디터가 열려 있으면 직접 봅니다. 토큰 0.

하루에 이런 확인 작업이 수십 번 있으면 토큰 절약이 상당합니다.
클로드 프로 구독 한도 내에서 더 오래, 더 많이 작업할 수 있습니다.

---

## 권장 레이아웃 설정 (5분)

**[화면: 안티그래비티 전체 화면]**

권장 레이아웃은 세 영역입니다.

**왼쪽 좁은 열 (20%):** 파일 탐색기
**가운데 열 (40%):** 터미널 (클로드 코드 실행)
**오른쪽 열 (40%):** 코드 에디터 (파일 내용 확인 및 직접 편집)

**설정 방법:**

1. 터미널 열기: `Ctrl + \``
2. 터미널 패널을 드래그해서 가운데 배치
3. 에디터 패널을 오른쪽에 배치
4. 왼쪽 파일 탐색기는 기본 상태 유지

또는 레이아웃 프리셋이 있다면 "Split Vertical" 옵션 사용.

---

## 이 레이아웃에서의 작업 흐름 (5분)

**[화면: 분할 완성 상태]**

실제 작업 흐름 예시입니다.

**단계 1 — 클로드에게 지시 (터미널)**

```
data/campaigns.csv를 분석해서 채널별 알오아이를 계산하고
scripts/analysis.py에 저장해줘.
```

**단계 2 — 에디터에서 확인 (오른쪽 패널)**

클로드가 파일을 생성하면 에디터에서 바로 내용을 확인합니다.
"코드 내용 보여줘" 같은 토큰 소모 없이 직접 읽습니다.

**단계 3 — 필요하면 직접 수정 (에디터)**

에디터에서 직접 코드를 수정합니다.
함수명 변경이나 작은 오류 수정은 클로드에게 맡기지 않고 직접 고칩니다.

**단계 4 — 클로드에게 다음 지시 (터미널)**

```
scripts/analysis.py를 실행해서 결과를 data/output/에 저장해줘.
```

이 흐름에서 클로드에게 물어보는 것은 실제 작업 지시뿐입니다.
파일 확인, 간단한 수정은 에디터로 직접 처리합니다.

---

## 토큰 절약 팁 정리 (2분)

안티그래비티와 클로드 코드를 함께 쓸 때 토큰을 절약하는 방법입니다.

**절약 방법 1:** 파일 내용 확인은 에디터로 직접 봅니다.
**절약 방법 2:** 단순 텍스트 수정(변수명, 오타)은 에디터에서 직접 합니다.
**절약 방법 3:** 클로드에게는 "판단이 필요한 작업"만 맡깁니다.
  - 알고리즘 설계, 에러 디버깅, 새 기능 작성

**클로드에게 맡기는 것:** 분석, 코드 생성, 디버깅
**직접 처리하는 것:** 파일 읽기, 간단한 수정, 결과 확인

---

## 직접 해보기

**목표**: 분할 레이아웃에서 파일 확인 없이 작업하는 흐름을 경험합니다.

**준비**
1. 안티그래비티에서 `~/ag-test` 폴더 열기
2. 터미널 + 에디터 분할 레이아웃 설정

**터미널에서**
```bash
cd ~/ag-test
claude
```

**클로드에게 이렇게 말합니다 (3번 연속)**
```
# 1번째 지시
채널별 알오아이 계산 파이썬 코드를 scripts/roi.py에 작성해줘.

# 에디터에서 roi.py 직접 확인 후...

# 2번째 지시 (내용 확인 없이)
scripts/roi.py 에 시각화 코드도 추가해줘. matplotlib 사용.

# 에디터에서 다시 확인 후...

# 3번째 지시
scripts/roi.py 를 실행해서 charts/roi.png 저장해줘.
```

→ 파일 확인을 에디터로 직접 하면서 클로드 코드 지시만 이어갑니다.

---

## 핵심 정리 (30초)

터미널(가운데) + 에디터(오른쪽) 분할 레이아웃이 핵심입니다.
파일 확인은 에디터로 직접, 클로드에게는 판단이 필요한 작업만 맡깁니다.
이 방식으로 클로드 프로 한도 내에서 더 많은 작업을 처리할 수 있습니다.

## 다음 클립 예고 (30초)

다음 클립에서는 안티그래비티 에이전트 매니저를 활용하는 방법을 다룹니다.
에이전트 매니저에서 여러 작업을 관리하고 결과를 모니터링하는 방법을 살펴보겠습니다.
