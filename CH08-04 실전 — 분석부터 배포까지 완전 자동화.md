---
tags: #CH08 #Antigravity #실전 #자동화
time: 17분
chapter: CH08
status: ✅ 대본완성
---

# CH08-04 실전 — 분석부터 배포까지 완전 자동화

## 📌 클립 정보
- **예상시간:** 17분
- **유형:** 화면 녹화 중심 (실습)
- **준비물:** CH08-01~03 선행 필수

---

## 학습 목표 선언 (30초)

이 클립을 마치면 한 가지를 할 수 있습니다.

하나의 프롬프트로 데이터 분석, 시각화 생성, 웹 배포까지 전체 파이프라인을 자동 실행할 수 있습니다.

---

## 전체 파이프라인 구조 (3분)

**[화면: 파이프라인 다이어그램]**

오늘 만들 파이프라인입니다:

```
1. 데이터 로드 및 검증
    ↓
2. 탐색적 데이터 분석 (EDA)
    ↓
3. 핵심 인사이트 도출
    ↓
4. 시각화 4종 생성 (막대, 라인, 파이, 히트맵)
    ↓
5. 대시보드 HTML 통합
    ↓
6. Netlify 자동 배포
    ↓
7. 공유 URL 생성
```

이 전체 과정을 Claude Code 하나의 프롬프트로 실행합니다.

---

## 실습 환경 준비 (3분)

**[화면: 터미널]**

프로젝트 폴더를 준비합니다:

```bash
mkdir ~/antigravity-pipeline && cd ~/antigravity-pipeline
mkdir -p data output themes
```

샘플 데이터를 준비합니다. 3개월간의 이커머스 데이터입니다:
- 일별 매출
- 카테고리별 판매량
- 고객 세그먼트별 구매 패턴

```bash
# 샘플 데이터는 강의 자료실에서 다운로드
cp ~/Downloads/패캠-실습자료/CH08/*.csv data/
```

---

## CLAUDE.md 파이프라인 정의 (4분)

**[화면: 에디터 — CLAUDE.md 작성]**

프로젝트 루트에 CLAUDE.md를 작성합니다:

```markdown
# 이커머스 분석 대시보드 파이프라인

## 프로젝트 목표
data/ 폴더의 CSV 파일들을 분석하여 대시보드를 생성하고 웹에 배포합니다.

## 데이터 파일
- data/daily_sales.csv — 일별 매출 데이터
- data/category_sales.csv — 카테고리별 판매
- data/customer_segments.csv — 고객 세그먼트

## 파이프라인 단계
1. 데이터 로드 및 품질 검증
2. 핵심 지표 계산 (총매출, 성장률, 상위 카테고리)
3. Antigravity로 시각화 4종 생성
4. output/dashboard.html로 통합
5. Netlify에 배포

## 시각화 사양
- 테마: themes/brand.json
- 차트 1: 일별 매출 추이 (라인)
- 차트 2: 카테고리별 매출 비중 (파이)
- 차트 3: 요일별 평균 매출 (막대)
- 차트 4: 카테고리 x 요일 히트맵

## 출력
- output/ 폴더에 모든 결과물 저장
- 최종 대시보드 URL 출력
```

---

## 한 문장으로 전체 실행 (5분)

**[화면: Claude Code 실행]**

```bash
claude
```

**클로드에게 이렇게 말합니다:**
```
CLAUDE.md에 정의된 파이프라인을 실행해줘.
데이터 분석부터 Netlify 배포까지 전부.
완료되면 대시보드 URL을 알려줘.
```

Claude Code가 자동으로:

1. **데이터 검증**: 결측치, 이상치 확인
2. **지표 계산**: 총매출 1.2억, 전월 대비 +15%
3. **시각화 생성**: 4개 차트 HTML 생성
4. **대시보드 통합**: 단일 HTML 페이지로 병합
5. **Netlify 배포**: `netlify deploy --prod`
6. **URL 출력**: https://your-dashboard.netlify.app

---

## 자동화 스케줄링 (2분)

**[화면: 크론 설정]**

매주 자동으로 대시보드를 업데이트하려면:

```bash
# 크론탭 편집
crontab -e

# 매주 월요일 오전 9시 실행
0 9 * * 1 cd ~/antigravity-pipeline && claude -p "CLAUDE.md 파이프라인 실행해줘"
```

새 데이터가 들어오면 자동으로 대시보드가 갱신됩니다.

---

## 직접 해보기

**목표**: 전체 파이프라인을 한 번에 실행합니다.

**준비**
```bash
cd ~/antigravity-pipeline
claude
```

**클로드에게 이렇게 말합니다**
```
CLAUDE.md 파이프라인을 실행해줘.
각 단계 진행상황을 알려주고,
완료되면 대시보드 URL을 공유해.
```

→ 전체 파이프라인이 자동 실행됩니다.
→ 완료 후 배포된 URL을 받습니다.

## 핵심 정리 (1분)

CLAUDE.md에 파이프라인을 정의하면 복잡한 작업도 한 문장으로 실행됩니다.
데이터 분석, 시각화, 배포까지 완전 자동화가 가능합니다.
크론으로 스케줄링하면 정기 리포트도 자동화됩니다.

## 다음 챕터 예고 (30초)

다음 챕터에서는 실제 마케팅 팀을 위한 대시보드를 처음부터 끝까지 만들어봅니다.
