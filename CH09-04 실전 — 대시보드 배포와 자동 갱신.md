---
tags: #CH09 #마케팅대시보드 #배포 #자동화
time: 14분
chapter: CH09
status: ✅ 대본완성
---

# CH09-04 실전 — 대시보드 배포와 자동 갱신

## 📌 클립 정보
- **예상시간:** 14분
- **유형:** 화면 녹화 중심
- **준비물:** CH09-03 선행 필수, Netlify 계정

---

## 학습 목표 선언 (30초)

이 클립을 마치면 두 가지를 할 수 있습니다.

첫 번째, 완성된 대시보드를 Netlify에 배포하고 URL을 팀에 공유할 수 있습니다.
두 번째, 매일 자동으로 데이터가 갱신되는 파이프라인을 구축할 수 있습니다.

---

## Netlify 배포 (4분)

**[화면: 터미널 — Netlify 배포]**

완성된 대시보드를 웹에 배포합니다:

```bash
cd ~/marketing-dashboard
netlify deploy --prod --dir=output
```

배포 완료 메시지:
```
✔ Deploy is live!
https://marketing-dashboard-abc123.netlify.app
```

이 URL을 팀에 공유하면 누구나 대시보드를 볼 수 있습니다.

**커스텀 도메인 연결 (선택):**
```bash
netlify domains:add dashboard.mycompany.com
```

---

## CLAUDE.md 전체 파이프라인 (4분)

**[화면: CLAUDE.md 작성]**

데이터 수집부터 배포까지 전체 과정을 CLAUDE.md에 정의합니다:

```markdown
# 마케팅 대시보드 자동화 파이프라인

## 실행 순서
1. 광고 데이터 수집 (Google, Meta, Naver)
2. 데이터 통합 및 KPI 계산
3. 대시보드 차트 4종 생성
4. HTML 통합
5. Netlify 배포
6. 완료 알림 발송

## 데이터 소스
- Google Ads API (config/google-ads.json)
- Meta Ads API (config/meta-ads.json)
- Naver Ads API (config/naver-ads.json)

## 출력
- output/dashboard.html → Netlify 배포
- 배포 URL을 Slack #marketing 채널에 공유

## 스케줄
- 매일 오전 8시 자동 실행
```

---

## 자동 갱신 설정 (4분)

**[화면: 크론 설정]**

매일 아침 자동으로 대시보드를 갱신합니다:

**스크립트 생성 (scripts/daily-dashboard.sh):**
```bash
#!/bin/bash
cd ~/marketing-dashboard
echo "$(date): 대시보드 갱신 시작" >> logs/cron.log

# Claude Code로 전체 파이프라인 실행
claude -p "CLAUDE.md 파이프라인을 실행해줘. 완료되면 Slack에 알려줘." >> logs/cron.log 2>&1

echo "$(date): 대시보드 갱신 완료" >> logs/cron.log
```

**크론 설정:**
```bash
chmod +x scripts/daily-dashboard.sh
crontab -e

# 매일 오전 8시 실행
0 8 * * * ~/marketing-dashboard/scripts/daily-dashboard.sh
```

출근하면 최신 대시보드가 준비되어 있습니다.

---

## Slack 알림 연동 (2분)

**[화면: Slack 웹훅 설정]**

대시보드 갱신 완료 시 Slack으로 알림을 보냅니다:

**CLAUDE.md에 추가:**
```markdown
## 알림 설정
- Slack 웹훅: config/slack-webhook.json
- 메시지 형식:
  "📊 마케팅 대시보드가 갱신되었습니다.
   - 총매출: {총매출}
   - ROAS: {평균ROAS}
   - 대시보드: {URL}"
```

매일 아침 팀 채널에 자동으로 메시지가 올라옵니다.

---

## 직접 해보기

**목표**: 전체 파이프라인을 한 번에 실행하고 배포합니다.

**터미널에서 실행**
```bash
cd ~/marketing-dashboard
claude
```

**클로드에게 이렇게 말합니다**
```
CLAUDE.md에 정의된 전체 파이프라인을 실행해줘.
데이터 수집부터 Netlify 배포까지.
완료되면 대시보드 URL을 알려줘.
```

**결과 확인**
- 브라우저에서 배포된 URL 접속
- 대시보드가 최신 데이터로 표시되는지 확인

## 핵심 정리 (1분)

Netlify로 대시보드를 웹에 배포하고 URL을 공유합니다.
CLAUDE.md에 전체 파이프라인을 정의하면 한 문장으로 실행됩니다.
크론으로 매일 자동 갱신하면 항상 최신 대시보드를 유지합니다.

## 다음 챕터 예고 (30초)

다음 챕터에서는 A/B 테스트 분석 대시보드를 만들어봅니다.
통계적 유의성 검정까지 자동화하는 방법을 배웁니다.
