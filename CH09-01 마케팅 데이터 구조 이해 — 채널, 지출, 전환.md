---
tags: #CH09 #마케팅 #데이터구조 #채널분석 #전환율 #프로젝트
time: 15분
chapter: CH09
status: ✅ 대본완성
---

# CH09-01 마케팅 데이터 구조 이해 — 채널, 지출, 전환

## 📌 클립 정보
- **예상시간:** 15분
- **유형:** 슬라이드 3장 + 화면 녹화
- **준비물:** 클로드 코드, 샘플 마케팅 데이터

---

## 학습 목표 선언 (30초)

이 클립을 마치면 두 가지를 할 수 있습니다.

첫 번째, 마케팅 데이터의 핵심 컬럼 구조와 지표를 설명할 수 있습니다.
두 번째, 클로드 코드에게 마케팅 데이터 전처리와 지표 계산을 정확히 지시할 수 있습니다.

---

## 마케팅 데이터의 기본 구조 (4분)

마케팅 분석에 사용하는 데이터는 보통 이 컬럼들로 구성됩니다.

| 컬럼명 | 의미 | 예시 |
|-------|------|-----|
| date | 집계 날짜 | 2024-01-15 |
| channel | 광고 채널 | SNS, 검색, 이메일, 디스플레이 |
| campaign | 캠페인명 | 신년 프로모션 |
| spend | 광고 지출 | 500,000원 |
| impressions | 노출수 | 150,000 |
| clicks | 클릭수 | 3,200 |
| conversions | 전환수 | 180 |
| revenue | 발생 매출 | 5,400,000원 |

---

## 핵심 마케팅 KPI 설계 (4분)

이 데이터에서 계산하는 핵심 지표입니다.

**알오아이 (ROI)**
`(매출 - 지출) / 지출 × 100`
광고비 대비 수익률. 높을수록 효율적.

**씨티알 (CTR, Click-Through Rate)**
`클릭수 / 노출수 × 100`
광고를 본 사람 중 클릭한 비율.

**씨브이알 (CVR, Conversion Rate)**
`전환수 / 클릭수 × 100`
클릭한 사람 중 실제 구매한 비율.

**씨피에이 (CPA, Cost Per Acquisition)**
`지출 / 전환수`
전환 1건당 광고비. 낮을수록 효율적.

**씨피씨 (CPC, Cost Per Click)**
`지출 / 클릭수`
클릭 1회당 광고비.

---

## 클로드 코드로 지표 계산 지시 (5분)

> "data/raw/campaigns.csv를 읽어서 다음 지표를 계산해줘. 알오아이, 씨티알, 씨브이알, 씨피에이, 씨피씨. 채널별로 집계하고 각 지표의 평균도 계산해줘. 결과를 data/output/kpi_by_channel.csv로 저장하고, 어떤 채널이 가장 효율적인지 한 문장으로 요약해줘."

---

## 직접 해보기

**목표**: 마케팅 샘플 데이터를 생성하고 핵심 KPI를 계산합니다.

**준비 (터미널에 복붙)**
```bash
mkdir ~/marketing-project && cd ~/marketing-project
mkdir -p data/raw data/output scripts charts reports
python3 -c "
import pandas as pd, numpy as np, random
random.seed(42)
dates = pd.date_range('2024-01-01', '2024-03-31', freq='D')
channels = ['SNS','검색','이메일','디스플레이','유튜브']
rows = []
for d in dates:
    for ch in channels:
        impr = random.randint(50000,200000)
        clicks = int(impr * random.uniform(0.01,0.05))
        conv = int(clicks * random.uniform(0.03,0.12))
        spend = random.randint(100000,600000)
        rev = conv * random.randint(20000,50000)
        rows.append({'date':str(d.date()),'channel':ch,'impressions':impr,'clicks':clicks,'conversions':conv,'spend':spend,'revenue':rev})
pd.DataFrame(rows).to_csv('data/raw/campaigns.csv',index=False)
print('샘플 데이터 생성 완료')
"
claude
```

**클로드에게 이렇게 말합니다**
```
data/raw/campaigns.csv를 읽어서 채널별 핵심 KPI를 계산해줘.
지표: ROI, CTR, CVR, CPA, CPC
채널별 집계 + 전체 평균
결과: data/output/kpi_by_channel.csv 저장
가장 효율적인 채널 한 줄 요약도 해줘.
```

---

## 핵심 정리 (30초)

마케팅 데이터의 핵심 컬럼은 채널, 지출, 노출, 클릭, 전환, 매출입니다.
알오아이, 씨티알, 씨브이알, 씨피에이, 씨피씨가 핵심 지표입니다.
클로드에게 지표 계산 공식을 명시하면 자동으로 계산합니다.

## 다음 클립 예고 (30초)

다음 클립에서는 시계열 분석을 통해 채널별 성과 트렌드를 시각화합니다.
