---
tags: #CH09 #마케팅 #자동갱신 #대시보드 #파이프라인
time: 17분
chapter: CH09
status: ✅ 대본완성
---

# CH09-03 자동 갱신 대시보드 — 새 데이터가 와도 버튼 하나로

## 📌 클립 정보
- **예상시간:** 17분
- **유형:** 화면 녹화
- **준비물:** CH09-01~02 실습 파일

---

## 학습 목표 선언 (30초)

이 클립을 마치면 두 가지를 할 수 있습니다.

첫 번째, 새 씨에스브이가 들어올 때마다 대시보드가 자동으로 갱신되는 구조를 설계할 수 있습니다.
두 번째, 완성된 마케팅 대시보드를 팀원들과 공유하는 방법을 적용할 수 있습니다.

---

## 자동 갱신 구조 설계 (4분)

매달 새로운 광고 데이터가 들어옵니다.
이걸 분석하고 대시보드를 업데이트하는 작업이 반복됩니다.

클로드 코드로 이 반복 작업을 자동화합니다.

**자동화 구조:**
```
새 씨에스브이 파일 → data/raw/ 에 저장
            ↓
클로드: "새 파일로 파이프라인 실행해줘" 한 줄 지시
            ↓
전처리 → KPI 계산 → 차트 생성 → 대시보드 생성 → 보고서
            ↓
최신 결과로 자동 업데이트
```

핵심은 CLAUDE.md에 파이프라인이 정의되어 있다는 것입니다.
"새 파일로 실행해줘" 한 마디로 전체 과정이 반복됩니다.

---

## 인터랙티브 대시보드 생성 지시 (6분)

> "charts/marketing_dashboard.html 을 만들어줘. 구성은 이렇게 해줘.
>
> 상단: KPI 카드 5개 — 총 지출, 총 매출, 평균 알오아이, 최고 채널, 최저 씨피에이.
>
> 중단 좌측: 채널별 알오아이 막대 차트 (플롯리, 인터랙티브).
> 중단 우측: 월별 매출 트렌드 라인 차트 (채널별 선, 범례 클릭 토글).
>
> 하단: 전체 데이터 테이블 — 날짜, 채널, 지출, 전환, 알오아이 — 정렬 가능.
>
> 상단에 날짜 범위 슬라이더 추가. 슬라이더 조작 시 모든 차트 동시 업데이트.
>
> 데이터는 data/raw/campaigns.csv. 스타일: 배경 #F8FAFC, 강조 #2563EB."

---

## 파이프라인 스크립트화 (4분)

다음 달에도 같은 작업을 하려면 스크립트로 저장합니다.

> "지금까지 한 분석 과정 전체를 scripts/run_pipeline.py로 저장해줘. 이 스크립트를 실행하면 data/raw/ 폴더에서 가장 최신 씨에스브이를 읽어서 KPI 계산, 차트 생성, 대시보드 생성, 보고서 작성을 순서대로 처리하게 해줘."

다음 달에는 이렇게만 합니다.

```bash
# 새 데이터를 data/raw/ 에 저장 후
claude
```
> "새 씨에스브이로 파이프라인 실행해줘."

---

## 직접 해보기

**목표**: 완성 대시보드 + 재사용 가능한 파이프라인 스크립트를 만듭니다.

**준비**
```bash
cd ~/marketing-project
claude
```

**클로드에게 이렇게 말합니다**
```
두 가지 해줘.

1. charts/marketing_dashboard.html
   - KPI 카드: 총지출, 총매출, 평균ROI, 최고채널
   - 좌: 채널별 ROI 막대 (인터랙티브)
   - 우: 월별 매출 트렌드 라인
   - 하단: 데이터 테이블 (정렬 가능)
   - 날짜 필터 드롭다운

2. scripts/run_pipeline.py
   data/raw/ 최신 CSV → KPI 계산 → 차트 → 대시보드 → 보고서
   전체 자동화 스크립트
```

**결과 확인**
```bash
open charts/marketing_dashboard.html
```

---

## 핵심 정리 (30초)

자동 갱신 구조의 핵심은 CLAUDE.md 파이프라인 정의와 재사용 가능한 스크립트입니다.
새 데이터가 오면 "새 파일로 실행해줘" 한 줄로 전체 과정이 반복됩니다.

## 챕터 마무리 (30초)

이것으로 CH09 마케팅 대시보드 프로젝트를 마칩니다.
다음 챕터에서는 에이비 테스트 결과를 자동으로 분석하고 시각화합니다.
