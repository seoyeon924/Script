---
tags: #패캠 #CH01 #Plan모드 #작업모드
time: 15분
chapter: CH01
status: ✅ 대본완성
---

# CH01-05 작업 모드 — Plan, Auto, Editor

## 📌 클립 정보
- **예상시간:** 15분
- **유형:** 슬라이드 3장 + 화면 녹화
- **준비물:** 여러 파일이 있는 샘플 프로젝트

---

## 오프닝 (1분)

파일 읽기·쓰기·실행 흐름을 익혔습니다.
그런데 큰 작업을 시킬 때 중간에 뭔가 잘못되면 어떻게 하나요?

클로드 코드에는 작업 방식을 바꾸는 세 가지 모드가 있습니다.
실행 전에 계획을 확인하는 Plan 모드, 승인 없이 자동으로 처리하는 Auto 모드, 에디터와 함께 쓰는 Editor 모드입니다.

---

## Plan 모드 (6분)

실행 전에 계획을 먼저 확인하는 방식입니다.

두 가지 방법이 있습니다.

**방법 1 — `/plan` 슬래시 커맨드**

```
/plan
전체 파이프라인 리팩토링해줘.
```

Claude가 실행 없이 계획만 출력합니다.

**방법 2 — 요청에 붙이기**

> "전체 파이프라인 리팩토링해줘. 실행 전에 계획 먼저 보여줘."

Claude 응답 예시:
```
계획:
1. scripts/preprocess.py — 날짜 처리 함수 분리
2. scripts/analyze.py — 중복 코드 3곳 통합
3. config.py 신규 생성 — 설정값 분리
4. README.md 업데이트

진행할까요?
```

"3번 빼고 나머지만 해줘"처럼 조정한 뒤 실행할 수 있습니다.

**체크포인트 — `/rewind`**

작업 중 잘못됐을 때 이전 상태로 되돌리는 공식 커맨드입니다.

```
/rewind
```

대화와 파일 변경 내용을 이전 시점으로 되돌립니다. `/checkpoint`도 같은 커맨드입니다.

단계별 작업이 중요할 때는 Git 커밋도 병행합니다.

> "전처리 완료되면 git commit 해줘. 커밋 메시지는 'chore: 전처리 완료'로."

```
전처리 완료  →  git commit -m "chore: 전처리 완료"
분석 완료    →  git commit -m "feat: 채널별 분석 완료"
시각화 오류  →  /rewind 또는 git checkout HEAD~1 (이전 시점 복구)
```

처음부터 다시 하지 않아도 됩니다.

---

## Auto 모드 (4분)

매 작업마다 승인을 누르지 않아도 되는 모드입니다.

자동화 파이프라인을 만들거나, 반복 작업이 많을 때 사용합니다.

```bash
claude --dangerously-skip-permissions
```

이 플래그를 붙이면 파일 생성, 코드 실행 등 모든 작업을 자동으로 승인합니다.

> ⚠️ 주의: 이 모드는 클로드가 요청 없이도 파일을 수정·삭제할 수 있습니다.
> 실수를 되돌리기 어려운 환경(프로덕션 서버, 원본 데이터)에서는 사용하지 마세요.
> 개발 환경 또는 테스트 폴더에서만 사용하는 것을 권장합니다.

**언제 쓰는가:**

- 샘플 데이터로 파이프라인 전체를 한 번에 돌릴 때
- 오류 없는 것으로 확인된 스크립트를 반복 실행할 때
- CI/CD 파이프라인에서 클로드 코드를 자동화 도구로 쓸 때

---

## Editor 모드 (3분)

터미널과 코드 에디터를 함께 쓰는 방식입니다.

클로드 코드는 터미널 단독으로도 쓸 수 있지만, VS Code 같은 에디터와 병행하면 더 효율적입니다.

**전형적인 화면 구성:**
```
[왼쪽] VS Code 에디터  |  [오른쪽] 터미널 (claude 실행 중)
```

클로드가 파일을 수정하면 VS Code에 실시간으로 반영됩니다.
에디터에서 직접 수정하고, 터미널에서 "방금 수정한 부분 테스트해줘"라고 이어서 지시할 수 있습니다.

**화면 분할 단축키 (맥 기준):**

| 방법 | 단축키 |
|------|--------|
| iTerm2 수직 분할 | `Cmd + D` |
| VS Code 통합 터미널 | `Ctrl + `` |
| 터미널 앱 탭 분리 | `Cmd + T` |

---

## 정리 (1분)

- **Plan 모드** — 실행 전 계획 검토 + Git 커밋으로 단계별 안전망
- **Auto 모드** — `--dangerously-skip-permissions` 플래그, 자동 승인 (테스트 환경에서만)
- **Editor 모드** — 터미널 + VS Code 병행, 실시간 파일 확인

다음 클립에서는 슬래시 커맨드 완전 정리입니다. 작업 중에 자주 쓰는 단축 명령어를 익힙니다.

---

## 🗒️ 서연 메모
- [ ] 여러 파일 수정 예제 준비 (3개 이상 파일)
- [ ] Git 있는 환경에서 진행 권장
- [ ] Auto 모드 시연은 테스트 폴더 사용

---

## 직접 해보기

**목표**: 실행 전에 계획을 먼저 받아서 위험한 변경을 방지합니다.

**클로드에게 이렇게 말합니다**
```
data/campaign.csv에서 비용이 250000 이상인 행만 남기고
나머지는 삭제해줘. 실행 전에 계획 먼저 보여줘.
```

→ 클로드가 실행하지 않고 계획만 출력합니다.
→ 계획을 확인한 후 "진행해줘" 또는 "취소해줘"를 입력합니다.

**Git 체크포인트 실습**
```
지금까지 작업 내용 git commit 해줘. 메시지는 "chore: 필터링 완료"로.
```

→ 클로드가 `git add -A && git commit -m "chore: 필터링 완료"`를 실행합니다.
→ `git log --oneline`으로 커밋이 생성됐는지 확인합니다.

**한 줄 팁**: 파일 여러 개를 바꾸는 작업은 항상 "실행 전에 계획 먼저 보여줘"를 붙이고, 완료 후 Git 커밋으로 기록을 남기세요.
