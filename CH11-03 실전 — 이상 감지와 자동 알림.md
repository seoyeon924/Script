---
tags: #CH11 #PM #이상감지 #알림
time: 16분
chapter: CH11
status: ✅ 대본완성
---

# CH11-03 실전 — 이상 감지와 자동 알림

## 📌 클립 정보
- **예상시간:** 16분
- **유형:** 화면 녹화 중심
- **준비물:** CH11-01~02 선행 필수

---

## 학습 목표 선언 (30초)

이 클립을 마치면 두 가지를 할 수 있습니다.

첫 번째, 지표 이상을 자동으로 감지하는 규칙을 설정할 수 있습니다.
두 번째, 이상 발생 시 Slack으로 자동 알림을 보낼 수 있습니다.

---

## 이상 감지 규칙 정의 (4분)

**[화면: CLAUDE.md 작성]**

어떤 상황을 "이상"으로 볼 것인지 정의합니다:

```markdown
# 지표 이상 감지 규칙

## DAU 이상
- 전일 대비 -15% 이상 하락 → 🚨 긴급 알림
- 전일 대비 -10% 이상 하락 → ⚠️ 주의 알림
- 7일 이동평균 대비 -20% → 🚨 긴급 알림

## 리텐션 이상
- Day 1 리텐션 25% 미만 → ⚠️ 주의 알림
- Day 7 리텐션 10% 미만 → 🚨 긴급 알림

## 전환율 이상
- 구매 전환율 2% 미만 → ⚠️ 주의 알림
- 결제 성공률 95% 미만 → 🚨 긴급 알림

## 알림 채널
- 긴급: Slack #product-alerts + 담당자 멘션
- 주의: Slack #product-metrics
```

---

## Claude Code로 이상 감지 (5분)

**[화면: Claude Code 실행]**

**클로드에게 이렇게 말합니다:**
```
data/daily_metrics.csv에서 이상 징후를 분석해줘.

체크할 규칙:
1. DAU 전일 대비 10% 이상 하락
2. 7일 이동평균 대비 15% 이상 하락
3. 구매 전환율 2% 미만

이상 발견되면 상세 내용을 알려주고,
output/anomaly-report.md로 저장해.
```

Claude Code의 분석 결과:
```
🚨 이상 감지 리포트

1. DAU 급락 감지
   - 어제 DAU: 12,450
   - 오늘 DAU: 10,340
   - 변화: -16.9% ❌
   - 원인 추정: 앱 업데이트 후 크래시 증가 가능성

2. 전환율 정상 범위
   - 현재: 2.8% ✅
   
권장 조치: 앱 크래시 로그 확인 필요
```

---

## Slack 자동 알림 설정 (4분)

**[화면: Slack 웹훅 연동]**

이상 감지 시 자동으로 Slack에 알림을 보냅니다:

**스크립트 작성 (scripts/alert-check.sh):**
```bash
#!/bin/bash
cd ~/pm-dashboard
claude -p "CLAUDE.md의 이상 감지 규칙으로 오늘 지표를 체크해. 이상 있으면 Slack #product-alerts에 알림 보내."
```

**크론 설정 (1시간마다 체크):**
```bash
0 * * * * ~/pm-dashboard/scripts/alert-check.sh
```

**Slack 메시지 예시:**
```
🚨 [긴급] DAU 이상 감지

📉 오늘 DAU가 전일 대비 16.9% 하락했습니다.
- 어제: 12,450명
- 오늘: 10,340명 (오전 11시 기준)

🔍 가능한 원인:
- 앱 버전 2.3.1 크래시 리포트 급증
- 서버 응답 시간 증가 (평균 2.1초 → 4.3초)

📋 대시보드: https://...
```

---

## 전체 파이프라인 (3분)

**[화면: 자동화 파이프라인]**

매시간 자동으로 실행되는 모니터링 파이프라인:

```
[매시간 크론 실행]
    ↓
[데이터 수집] → 최신 지표 가져오기
    ↓
[이상 감지] → 규칙 기반 체크
    ↓
[정상] → 로그 기록 후 종료
[이상] → Slack 알림 + 리포트 생성
```

PM이 자고 있어도 시스템이 지표를 모니터링합니다.

---

## 직접 해보기

**목표**: 이상 감지 알림 시스템을 구축합니다.

**터미널에서 실행**
```bash
cd ~/pm-dashboard
claude
```

**클로드에게 이렇게 말합니다**
```
지표 이상 감지 시스템을 만들어줘.

규칙:
- DAU 전일 대비 -10% 이상이면 알림
- 구매 전환율 2% 미만이면 알림

샘플 데이터로 테스트하고,
이상이 감지되면 Slack 메시지 형식으로 출력해줘.
실제 Slack 전송은 안 해도 되고, 메시지만 보여줘.
```

**출력 예시 확인**
```
[Slack 알림 미리보기]
🚨 DAU 이상 감지
...
```

## 핵심 정리 (1분)

이상 감지 규칙을 CLAUDE.md에 명확히 정의합니다.
크론으로 정기 실행하면 24시간 자동 모니터링됩니다.
Slack 연동으로 이상 발생 시 즉시 알림을 받습니다.

## 다음 챕터 예고 (30초)

다음 챕터에서는 리텐션과 매출 분석 대시보드를 만들어봅니다.
코호트 분석과 LTV 계산을 자동화합니다.
