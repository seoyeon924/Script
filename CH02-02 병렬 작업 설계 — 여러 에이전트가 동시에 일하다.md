---
tags: #CH02 #병렬처리 #설계패턴
time: 20분
chapter: CH02
status: ✅ 대본완성
---

# CH02-02 병렬 작업 설계 — 여러 에이전트가 동시에 일하다

## 📌 클립 정보
- **예상시간:** 20분
- **유형:** 슬라이드 4장 + 화면 녹화
- **준비물:** CH02-01 선행 필수, 샘플 데이터 파일

---

## 학습 목표 선언 (30초)

이 클립을 마치면 두 가지를 할 수 있습니다.

첫 번째, 병렬 작업 설계의 3가지 패턴을 상황에 맞게 선택할 수 있습니다.
두 번째, 클로드엠디에 병렬 작업 구조를 정의해서 서브에이전트를 체계적으로 운영할 수 있습니다.

---

## 병렬 처리가 가져오는 이점 (3분)

**[화면: 순차 처리 vs 병렬 처리 타임라인]**

예를 들어 월별 판매 데이터 12개 파일이 있고, 각 파일 분석에 2분이 걸린다고 가정합니다.

순차 처리라면 12 × 2 = 24분이 필요합니다.
병렬 처리로 12개를 동시에 실행하면 이론상 2분이면 됩니다.

실제로는 시스템 리소스와 에이피아이 동시 요청 제한이 있어서 정확하게 12배가 되지는 않습니다.
그러나 대규모 작업에서 시간을 크게 단축할 수 있습니다.

---

## 병렬 설계 3가지 패턴 (6분)

**[화면: 패턴별 구조 도식]**

### 패턴 1 — 분할·처리·합산

입력 데이터를 나눠서 각 에이전트가 독립 처리하고, 메인 에이전트가 결과를 합산합니다.

```
메인 에이전트
├── 서브에이전트 A → jan.csv 분석 → jan_result.json
├── 서브에이전트 B → feb.csv 분석 → feb_result.json
└── 서브에이전트 C → mar.csv 분석 → mar_result.json
        ↓
메인 에이전트 → 3개 결과 합산 → 분기별 최종 리포트
```

가장 흔하게 사용하는 패턴입니다.
대용량 데이터 분석, 여러 파일 처리에 적합합니다.

### 패턴 2 — 역할 분리 파이프라인

에이전트마다 전담 역할을 부여하고, 결과를 순서대로 전달합니다.

```
데이터 수집 에이전트 → raw_data.csv
        ↓
전처리 에이전트 → cleaned_data.csv
        ↓
분석 에이전트 → analysis_result.json
        ↓
리포트 에이전트 → final_report.pdf
```

각 단계가 이전 단계 결과에 의존할 때 사용합니다.
단, 이 경우는 순서가 있으므로 완전한 병렬이 아닙니다.
각 단계 내부를 병렬화하는 방식으로 속도를 높입니다.

### 패턴 3 — 병렬 검토 (앙상블)

같은 작업을 여러 에이전트가 독립적으로 처리하고 결과를 비교합니다.

```
메인 에이전트
├── 에이전트 A → 분석 결과 A
├── 에이전트 B → 분석 결과 B
└── 에이전트 C → 분석 결과 C
        ↓
메인 에이전트 → 3개 비교 → 최적 결과 선택 또는 합의
```

중요한 의사결정, 정확도가 높아야 하는 작업에 사용합니다.
여러 관점에서 검토가 필요한 경우에 유용합니다.

---

## 클로드엠디에 병렬 구조 정의하기 (5분)

**[화면: 클로드엠디 작성 시연]**

서브에이전트를 체계적으로 운영하려면 클로드엠디에 구조를 명시해두는 것이 중요합니다.

아래는 판매 데이터 분석 프로젝트의 클로드엠디 예시입니다.

```markdown
# 판매 데이터 분석 프로젝트

## 프로젝트 개요
월별 판매 데이터를 분석하여 채널별 성과와 상품별 트렌드를 파악합니다.

## 폴더 구조
- data/monthly/     : 월별 원본 씨에스브이 (수정 금지)
- data/processed/   : 전처리 결과
- output/results/   : 분석 결과 (파일명: YYYY_MM_result.json)
- output/reports/   : 최종 리포트

## 서브에이전트 운영 규칙
- 각 서브에이전트는 월별 파일 하나씩 담당
- 결과는 반드시 output/results/에 저장
- 파일명 형식: YYYY_MM_result.json
- 원본 데이터 절대 수정 금지

## 분석 항목 (각 서브에이전트 공통)
1. 총 판매량 및 매출
2. 채널별 비중 (온라인/오프라인/파트너)
3. 상위 10개 상품
4. 전월 대비 증감 (데이터 있는 경우)
```

이렇게 클로드엠디를 작성하면 각 서브에이전트에게 별도 지시를 길게 작성하지 않아도 됩니다.
클로드엠디를 읽고 규칙에 따라 처리합니다.

---

## 병렬 작업 설계 시 주의사항 (4분)

**[화면: 주의사항 체크리스트]**

**첫 번째, 파일 충돌을 방지합니다.**

여러 서브에이전트가 같은 파일에 동시 쓰기를 시도하면 충돌이 발생합니다.
각 서브에이전트가 독립된 출력 파일을 갖도록 설계합니다.
파일명에 에이전트 구분자나 데이터 구분자를 포함합니다.
예: `jan_result.json`, `feb_result.json` (공통 `result.json` 금지)

**두 번째, 컨텍스트 한계를 고려합니다.**

서브에이전트도 컨텍스트 한계가 있습니다.
하나의 서브에이전트에게 너무 많은 데이터를 처리하게 하면 오류가 납니다.
대용량 파일은 청크(chunk)로 나눠서 처리하는 것을 권장합니다.

**세 번째, 오류 처리 전략을 세웁니다.**

일부 서브에이전트가 실패해도 전체 작업이 중단되지 않도록 합니다.
실패한 서브에이전트를 확인하는 로직을 메인 에이전트에 포함합니다.
예: "모든 서브에이전트 완료 후, 실패한 파일이 있으면 목록 출력해줘"

---

## 심화 — Git Worktree로 클로드 코드 여러 개 동시 실행 (4분)

**[화면: 터미널 2개 분할]**

지금까지 배운 서브에이전트 방식은 클로드 코드 한 세션 안에서 작업을 위임하는 방식입니다.
이번에는 다른 접근입니다. **클로드 코드 세션 자체를 여러 개** 동시에 띄우는 방법입니다.

---

### 왜 이 방법이 필요한가

이런 상황을 상상해보세요.

클로드에게 뭔가 시키고 → 멍하니 기다리고
끝나면 다음 거 시키고 → 또 기다리고

이렇게 하면 결국 내가 클로드 속도에 맞춰서 살게 됩니다.

Git Worktree를 사용하면 클로드 코드 여러 개를 각각 다른 폴더에서 동시에 돌릴 수 있습니다.
한 클로드가 작업하는 동안, 다른 클로드에게 다음 작업을 시킬 수 있습니다.

---

### Git Worktree란

깃(Git) 기본 기능입니다. 별도 설치가 필요 없습니다.

원본 프로젝트를 그대로 두고, 폴더를 여러 개 분리해서 각자 독립적으로 작업합니다.
각 폴더가 완전히 분리되어 있기 때문에, 여러 클로드가 동시에 작업해도 파일 충돌이 없습니다.

---

### 실제 사용법

**1단계 — 워크트리 생성**

깃이 있는 프로젝트 폴더에서 시작합니다.

```bash
# 현재 프로젝트 루트에서
git worktree add ../프로젝트-A 작업브랜치-A
git worktree add ../프로젝트-B 작업브랜치-B
```

`프로젝트-A`, `프로젝트-B` 폴더가 새로 생성됩니다.
각각 독립된 깃 브랜치 환경입니다.

**2단계 — 터미널 2개 열어서 각각 클로드 실행**

```bash
# 터미널 1
cd ../프로젝트-A
claude
# 클로드에게 작업 A 지시
```

```bash
# 터미널 2
cd ../프로젝트-B
claude
# 클로드에게 작업 B 지시
```

두 클로드가 동시에 작업합니다. 폴더가 분리되어 있어서 충돌이 없습니다.

**3단계 — 완료 후 병합**

```bash
# 원본 프로젝트로 돌아와서
cd 원본프로젝트
git merge 작업브랜치-A
git merge 작업브랜치-B

# 워크트리 제거
git worktree remove ../프로젝트-A
git worktree remove ../프로젝트-B
```

**현재 워크트리 목록 확인:**
```bash
git worktree list
```

---

### 서브에이전트 vs Git Worktree

| 방식 | 특징 | 언제 쓰나 |
|------|------|---------|
| 서브에이전트 | 한 세션 안에서 자동 위임 | 같은 프로젝트, 연속된 작업 |
| Git Worktree | 세션 자체를 여러 개 | 완전히 독립적인 작업을 동시에 |

두 방법을 조합할 수도 있습니다.
각 워크트리에서 클로드를 실행하고, 그 클로드 안에서 서브에이전트를 사용하는 방식입니다.

---

## 화면 실습 — 12개 월별 파일 병렬 분석 (5분)

**[화면: 터미널]**

```bash
claude
```

지시 예시입니다.

> "data/monthly/ 폴더에 있는 씨에스브이 파일 목록을 먼저 확인하고, 각 파일을 서브에이전트로 병렬 처리해줘. 클로드엠디 규칙에 따라 output/results/에 각각 저장하고, 전체 완료 후 성공/실패 파일 목록을 알려줘."

클로드 코드가 먼저 파일 목록을 확인합니다.
그 다음 각 파일에 대한 Task 도구를 생성해서 병렬 실행합니다.
완료 후 성공/실패 현황을 보고합니다.

---

---

## 직접 해보기

**목표**: 클로드엠디에 병렬 작업 규칙을 정의하고, 클로드가 자동으로 따르는 것을 확인합니다.

**클로드에게 이렇게 말합니다**
```
이 프로젝트에서 여러 씨에스브이를 병렬 처리하는 규칙을
CLAUDE.md에 추가해줘.
각 파일은 별도 서브에이전트로 처리하고,
결과는 output/results/{파일명}_result.json으로 저장하는 규칙이야.
```

→ 클로드가 CLAUDE.md에 병렬 처리 규칙을 직접 추가합니다.
→ 이후 "파일들 분석해줘"라고만 해도 규칙대로 실행합니다.

## 핵심 정리 (1분)

병렬 설계의 핵심은 작업을 독립적인 단위로 나누는 것입니다.
패턴 1(분할-처리-합산)은 같은 작업 반복, 패턴 2(파이프라인)는 순차 의존 작업, 패턴 3(병렬 검토)은 검토·검증 목적에 사용합니다.
클로드엠디에 병렬 구조를 명시하면 서브에이전트가 자율적으로 규칙을 따릅니다.

## 다음 클립 예고 (30초)

다음 클립에서는 실제 마케팅 데이터 파이프라인을 서브에이전트로 처음부터 끝까지 자동화하는 실습을 진행합니다.
