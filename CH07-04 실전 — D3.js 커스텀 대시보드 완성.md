---
tags: #CH07 #D3.js #대시보드 #실전 #커스텀시각화완성
time: 17분
chapter: CH07
status: ✅ 대본완성
---

# CH07-04 실전 — D3.js 커스텀 대시보드 완성

## 📌 클립 정보
- **예상시간:** 17분
- **유형:** 화면 녹화 위주 (실전 실습)
- **준비물:** CH07-01~03 실습 파일

---

## 대시보드 설계 원칙 (3분)

여러 차트를 한 화면에 넣을 때 세 가지 원칙이 있습니다.

**첫 번째, 위계 구조입니다.**
상단: 핵심 케이피아이 수치 카드 (숫자만)
중단: 주요 차트 1~2개 (가장 중요한 인사이트)
하단: 보조 차트 (세부 분석)

**두 번째, 차트 간 연동입니다.**
한 차트에서 항목을 클릭하면 다른 차트가 해당 항목 기준으로 필터링됩니다.
이것을 크로스 필터링이라고 합니다.

**세 번째, 반응형 레이아웃입니다.**
화면 너비에 따라 차트 크기가 자동으로 조정됩니다.

---

## 클로드에게 대시보드 생성 지시 (6분)

이전 클립에서 만든 차트들을 하나로 통합합니다.

> "새 파일 charts/dashboard_d3.html을 만들어줘. 세 구역으로 구성해줘.
>
> 상단 구역: KPI 카드 4개. 총 매출, 평균 알오아이, 최고 채널, 전환율. 각각 큰 숫자와 전월 대비 증감 화살표 표시.
>
> 중단 구역: 채널별 매출 막대 차트 (왼쪽), 월별 트렌드 라인 차트 (오른쪽). 두 차트를 좌우로 배치.
>
> 하단 구역: 코호트 히트맵 전체 너비.
>
> 연동 기능: 막대 차트에서 채널 클릭하면 라인 차트가 해당 채널만 표시하도록 업데이트.
>
> 스타일: 배경 #F8FAFC, 카드 배경 흰색, 강조색 #2563EB, 폰트 나눔고딕. 모든 차트 애니메이션 포함.
>
> 데이터는 data/ 폴더의 기존 파일들 사용."

에이전트가 기존 차트 코드를 재사용하고 통합 레이아웃으로 조합합니다.

---

## 차트 간 연동 원리 (3분)

크로스 필터링의 동작 방식입니다.

막대를 클릭하면 자바스크립트 이벤트가 발생합니다.
이 이벤트가 전역 변수에 선택된 채널 값을 저장합니다.
라인 차트는 이 변수를 감시하다가 값이 바뀌면 데이터를 필터링해서 다시 그립니다.

클로드 코드에게 이 방식을 지시로 전달합니다.

> "막대 클릭 이벤트에서 선택 채널을 전역 변수 `selectedChannel`에 저장하고, 라인 차트의 `update()` 함수를 호출해서 필터링된 데이터로 다시 그려줘. 선택 초기화 버튼도 추가해줘."

---

## 직접 해보기

**목표**: 크로스 필터링이 있는 완성 대시보드를 만듭니다.

**준비 (터미널에 복붙)**
```bash
cd ~/d3-test
claude
```

**클로드에게 이렇게 말합니다**
```
charts/dashboard_d3.html 을 만들어줘.

레이아웃:
- 상단: KPI 카드 (총매출, 채널수, 최고채널)
- 중단 좌측: 채널별 매출 막대 차트
- 중단 우측: 월별 매출 라인 차트
- 하단: data/cohort.csv 기반 코호트 히트맵

연동:
- 막대 클릭 → 라인 차트 해당 채널만 필터링
- 초기화 버튼 (우측 상단)

스타일: 배경 #F8FAFC, 강조 #2563EB, 애니메이션 포함
데이터: data/ 폴더 기존 파일 사용
```

**결과 확인**
```bash
open charts/dashboard_d3.html
```

→ 막대 클릭 시 오른쪽 라인 차트가 업데이트되는지 확인합니다.

---

## 핵심 정리 (30초)

디쓰리제이에스 대시보드는 케이피아이 카드 + 주요 차트 + 보조 차트 구조로 설계합니다.
차트 간 연동은 전역 변수와 업데이트 함수로 구현합니다.
클로드 코드에게 레이아웃, 연동 방식, 스타일을 한 번에 지시하면 완성됩니다.

## 챕터 마무리 (30초)

이것으로 CH07 디쓰리제이에스 커스텀 시각화를 마칩니다.
다음 챕터에서는 클로드 코드와 안티그래비티를 함께 활용한 시각화 자동화 파이프라인을 다룹니다.
