---
tags: #CH08 #안티그래비티 #Antigravity #설치 #연결 #환경설정
time: 15분
chapter: CH08
status: ✅ 대본완성
---

# CH08-01 안티그래비티 설치와 클로드 코드 연결

## 📌 클립 정보
- **예상시간:** 15분
- **유형:** 화면 녹화 (브라우저 + 에디터)
- **준비물:** 맥 애플 실리콘 (M1 이상), 클로드 프로 이상 구독

---

## 학습 목표 선언 (30초)

이 클립을 마치면 두 가지를 할 수 있습니다.

첫 번째, 안티그래비티를 설치하고 클로드 코드 계정을 연결할 수 있습니다.
두 번째, 안티그래비티 내장 터미널에서 클로드 코드를 실행할 수 있습니다.

---

## 안티그래비티란 무엇인가 (3분)

안티그래비티는 구글이 만든 에이아이 코드 에디터입니다.
에디터와 에이전트 매니저가 하나의 앱에 통합되어 있습니다.

클로드 코드와 함께 쓰면 두 가지 장점이 있습니다.

**첫 번째, 에이피아이 토큰 절약입니다.**
클로드 프로 구독이 있으면 에이피아이 키 없이 연결됩니다.
토큰 사용량이 구독 내에 포함되어 별도 과금이 없습니다.

**두 번째, 에디터와 터미널 동시 사용입니다.**
왼쪽 터미널에서 클로드 코드를 실행하고, 오른쪽 에디터에서 결과 파일을 바로 확인합니다.
파일 내용을 확인하려고 클로드에게 물어볼 필요가 없습니다.
그만큼 토큰을 아낍니다.

**지원 환경:**
- 맥: 애플 실리콘(M1/M2/M3/M4), 맥오에스 12 몬터레이 이상
- 윈도우: 윈도우 10 64비트 이상
- 인텔 맥: 지원하지 않습니다

---

## 설치 방법 (4분)

**[화면: 브라우저]**

`https://antigravity.google/download` 에 접속합니다.

맥 또는 윈도우 버전을 내려받습니다.
설치 파일을 실행합니다.
설치 완료 후 안티그래비티를 실행합니다.

---

## 클로드 코드 익스텐션 설치 및 계정 연결 (6분)

**[화면: 안티그래비티 에디터]**

**1단계: 익스텐션 설치**

왼쪽 사이드바에서 익스텐션 탭을 클릭합니다.
검색창에 "Claude"를 입력합니다.
클로드 코드 익스텐션을 찾아서 설치합니다.
설치 완료 후 `Enable` 버튼을 클릭합니다.

**2단계: 클로드 아이콘 클릭**

왼쪽 사이드바에 주황 꽃 모양의 클로드 아이콘이 생깁니다.
이 아이콘을 클릭합니다.
클로드 코드 패널이 열립니다.

**3단계: 계정 연결**

패널에서 "Connect Account" 또는 로그인 버튼을 클릭합니다.
클로드 프로 이상 구독이 있으면 이 방식으로 연결합니다.

브라우저에서 `claude.ai` 인증 페이지가 열립니다.
페이지에 인증 코드가 표시됩니다.
이 코드를 복사합니다.
안티그래비티 패널의 입력창에 붙여넣습니다.
연결 완료 메시지가 나타납니다.

---

## 내장 터미널에서 클로드 코드 실행 (1분)

**[화면: 안티그래비티]**

터미널을 엽니다. 단축키는 `Ctrl + \`` 입니다.
터미널에서 프로젝트 폴더로 이동합니다.

```bash
cd ~/my-analysis
claude
```

터미널 왼쪽에서 클로드 코드가 실행되고, 오른쪽 에디터에서 파일 변경 내용이 실시간으로 표시됩니다.

---

## 직접 해보기

**목표**: 안티그래비티에서 클로드 코드를 실행하고 파일이 에디터에 표시되는지 확인합니다.

**순서**
1. 안티그래비티에서 익스텐션 탭 → "Claude" 검색 → 설치 → Enable
2. 주황 꽃 아이콘 클릭 → 계정 연결 (URL 코드 복붙)
3. `Ctrl + \`` 로 터미널 열기
4. 터미널에서 아래 입력:

```bash
mkdir ~/ag-test && cd ~/ag-test
claude
```

**클로드에게 이렇게 말합니다**
```
이 폴더에 test.py 파일 만들어줘.
내용: 채널별 알오아이를 계산하는 간단한 파이썬 코드.
```

→ 오른쪽 에디터에서 test.py 파일이 생성되는 것을 확인합니다.

---

## 핵심 정리 (30초)

안티그래비티 설치 → 익스텐션에서 Claude 검색 설치 Enable → 주황 꽃 아이콘 → 계정 연결 (URL 코드 복붙) 순서입니다.
연결 후 내장 터미널에서 `claude` 를 실행하면 에디터와 동시에 사용할 수 있습니다.

## 다음 클립 예고 (30초)

다음 클립에서는 안티그래비티 화면을 최적으로 분할하는 방법과 에디터에서 클로드 코드 작업을 모니터링하는 방법을 다룹니다.
