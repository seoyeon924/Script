---
title: CH01-07 서브에이전트 — 역할 분리와 위임
chapter: CH01
clip: 07
time: 12분
type: 슬라이드 + 실습
---

# CH01-07 서브에이전트 — 역할 분리와 위임

## 클립 목표
나만의 서브에이전트를 직접 만들어서 역할을 분리하는 법을 다룹니다.

> 서브에이전트가 무엇인지, 병렬 처리는 어떻게 하는지는 CH02에서 자세히 다룹니다.
> 여기서는 "내 프로젝트에 맞는 전담 에이전트"를 실제로 만드는 방법에 집중합니다.

---

## 커스텀 서브에이전트 만들기 (7분)

**저장 위치:**
- 전체 공통: `~/.claude/agents/`
- 특정 프로젝트: `.claude/agents/`

파일명이 에이전트 이름이 됩니다.

**기본 형식 (마크다운):**

```markdown
---
name: 데이터-정리
description: CSV 파일의 형식 오류를 찾아 수정하는 에이전트
model: claude-haiku-4-5
tools:
  - Read
  - Write
---

당신은 CSV 데이터 정리 전문가입니다.

## 역할
- CSV 파일의 날짜 형식을 YYYY-MM-DD로 통일합니다
- 빈 값(null)을 0 또는 "없음"으로 채웁니다
- 중복 행을 제거합니다
- 처리 후 변경 내역을 요약합니다

## 규칙
- 원본 파일은 수정하지 않습니다
- 처리된 파일은 data/processed/ 에 저장합니다
```

**데이터 분석용 서브에이전트 예시:**

```markdown
---
name: 보고서-작성
description: 분석 결과를 바탕으로 마크다운 보고서를 작성하는 에이전트
model: claude-sonnet-4-5
tools:
  - Read
  - Write
---

당신은 데이터 분석 보고서 작성 전문가입니다.

## 보고서 형식
1. 요약 (3줄 이내)
2. 주요 지표 테이블
3. 채널별 분석
4. 특이사항
5. 다음 주 권고사항

## 규칙
- 모든 수치는 원화(KRW) 표시
- 증감률은 소수점 1자리까지
- 저장 경로: reports/weekly_YYYY-MM-DD.md
```

---

## 4. 서브에이전트 호출 방법

메인 세션에서 자연어로 요청하면 클로드가 자동으로 적합한 서브에이전트를 선택합니다.

또는 명시적으로 지정할 수도 있습니다:
```
@데이터-정리 를 이용해서 sales_march.csv 정리해줘
```

---

## 직접 해보기

**목표**: `/agents` 명령어로 서브에이전트를 만들고 역할을 분리해서 사용합니다.

### 방법 1 — `/agents` 메뉴 (공식 방법)

```bash
claude
```

```
/agents
```

→ 서브에이전트 관리 메뉴가 열립니다.
→ "Create new agent" → "User-level" (모든 프로젝트에서 사용) 또는 "Project-level" (.claude/agents/ 폴더에 저장)
→ 이름, 역할 설명, 허용 도구, 모델을 인터랙티브하게 설정합니다.

### 방법 2 — 파일 직접 생성 (터미널에 복붙)

```bash
# 프로젝트 서브에이전트 폴더 생성
mkdir -p .claude/agents

# 데이터 분석 서브에이전트 파일 작성
cat > .claude/agents/data-analyst.md << 'EOF'
---
name: data-analyst
description: CSV 파일을 분석해서 기초 통계를 계산합니다. 데이터 분석 요청 시 자동으로 사용됩니다.
tools: Read, Bash, Write
model: sonnet
---

당신은 데이터 분석 전문가입니다.
CSV 파일을 읽고 행 수, 컬럼 목록, 수치 컬럼의 평균/최대/최소를 계산합니다.
결과는 마크다운 표로 정리합니다.
EOF
```

### 서브에이전트 활용

```
data-analyst 서브에이전트를 사용해서 data/campaign.csv를 분석해줘
```

→ 클로드가 data-analyst 서브에이전트에 위임해서 분석을 수행합니다.

**서브에이전트 목록 확인**
```bash
# 터미널에서
claude agents
```

---

## 핵심 정리

- 서브에이전트는 특정 역할을 담당하는 독립 에이전트입니다.
- `.claude/agents/` 폴더에 마크다운 파일로 만듭니다.
- 역할, 사용할 도구, 모델을 명시합니다.
- 복잡한 작업을 역할별로 분리하면 품질과 비용 모두 개선됩니다.

다음 클립에서는 MCP(Model Context Protocol)로 외부 도구와 클로드를 연결하는 방법을 다룹니다.
