---
title: CH01-04 슬래시 커맨드 완전 정리
chapter: CH01
clip: 04
time: 10분
type: 슬라이드 + 데모
---

# CH01-04 슬래시 커맨드 완전 정리

## 클립 목표
클로드 코드 인터랙티브 모드에서 자주 쓰는 슬래시 커맨드를 익힙니다.

---

## 1. 슬래시 커맨드란 (1분)

클로드 코드 대화 중에 `/`로 시작하는 명령어를 입력하면, 특정 기능을 즉시 실행합니다.
채팅에서 "이전 내용 요약해줘"라고 말하는 것보다, `/compact`를 입력하는 것이 정확하고 빠릅니다.

대화창에서 `/`만 입력하면 사용 가능한 명령어 목록이 자동완성으로 나타납니다.

---

## 2. 필수 슬래시 커맨드 (6분)

### 컨텍스트 관리

**`/compact`**
대화 내용을 요약해서 컨텍스트 크기를 줄입니다.
대화가 길어지면 토큰 사용량이 늘어납니다. `/compact`를 실행하면 핵심만 남기고 정리합니다.
비유하자면, 회의록을 전체 녹취록 대신 요점 정리로 교체하는 것과 같습니다.

```
/compact
```

집중할 내용을 지정할 수도 있습니다.

```
/compact 코드 변경 사항 위주로 정리해줘
```

**`/clear`**
대화 히스토리를 완전히 초기화합니다.
새 작업을 시작할 때 이전 내용이 방해가 되는 경우 사용합니다.
별칭: `/reset`, `/new`

```
/clear
```

**`/context`**
현재 컨텍스트 창 사용량을 시각적으로 보여줍니다.
80% 이상 차면 `/compact`나 대화 종료를 고려하세요.

```
/context
```

### 변경 내용 확인

**`/diff`**
클로드가 변경한 파일의 차이점을 인터랙티브 뷰어로 보여줍니다.
좌우 화살표로 커밋 전 차이와 클로드 턴별 변경 내역을 전환합니다.

```
/diff
```

### 비용 확인

**`/cost`**
현재 세션에서 사용한 토큰 비용을 확인합니다.

```
/cost
```

### 설정 및 정보

**`/config`**
설정 화면을 엽니다. 모델 선택, 테마, 출력 스타일 등을 변경합니다.
별칭: `/settings`

```
/config
```

**`/help`**
사용 가능한 슬래시 커맨드 전체 목록을 출력합니다.

```
/help
```

**`/init`**
현재 프로젝트를 분석해서 클로드엠디 파일을 자동 생성합니다.

```
/init
```

### 모델 및 모드 전환

**`/fast [on|off]`**
빠른 응답 모드를 켜고 끕니다.
단순한 작업에는 패스트 모드가 유용합니다. 복잡한 분석은 일반 모드를 권장합니다.

```
/fast on
/fast off
```

**`/model`**
사용 중인 에이아이 모델을 변경합니다.

```
/model
```

### 대화 분기

**`/fork [이름]`**
현재 대화 지점에서 새 분기를 만듭니다.
여러 방향을 시험해보거나 위험한 변경 전에 현재 상태를 보존할 때 사용합니다.

```
/fork
/fork 리팩토링-시도
```

### 세션 관리

**`/rename [이름]`**
현재 세션에 이름을 붙입니다.
나중에 `claude --resume 이름`으로 이어서 시작할 수 있습니다.

```
/rename 마케팅분석-2월
```

**`/exit`**
클로드 코드를 종료합니다. 별칭: `/quit`

```
/exit
```

---

## 3. 단축키 (3분)

슬래시 커맨드 외에 키보드 단축키도 있습니다.

| 단축키 | 기능 |
|--------|------|
| `Ctrl+C` | 현재 작업 중단 |
| `Ctrl+D` | 세션 종료 |
| `Ctrl+L` | 터미널 화면 정리 (대화 히스토리는 유지) |
| `Ctrl+R` | 이전에 입력한 명령어 검색 |
| `Esc, Esc` | 진행 중인 작업 중단 후 이전 상태로 |

`Esc, Esc` (이스케이프 두 번)는 클로드가 작업 도중 잘못된 방향으로 가고 있을 때 즉시 중단하는 가장 빠른 방법입니다.

---

## 직접 해보기

**목표**: 가장 자주 쓰는 슬래시 커맨드 3가지를 직접 실행합니다.

**클로드 코드 세션을 시작합니다**
```bash
claude
```

**1. 사용 가능한 커맨드 목록 확인**
```
/help
```

**2. 현재 세션 비용 확인 (토큰 사용량)**
```
/cost
```

**3. 컨텍스트를 초기화하고 새로 시작**
```
/clear
지금부터 새 분석 작업 시작할게. data/campaign.csv 읽어줘.
```

→ `/clear` 후에는 이전 대화 내용을 기억하지 않습니다.
→ 새로운 작업을 시작할 때 컨텍스트를 깨끗하게 만드는 습관을 들이면 토큰 절약이 됩니다.

---

## 핵심 정리

자주 쓰는 슬래시 커맨드 4가지만 기억합니다.

1. `/compact` — 대화가 길어지면 컨텍스트 압축
2. `/clear` — 새 작업 시작 시 초기화
3. `/diff` — 변경 내용 확인
4. `/cost` — 토큰 비용 확인

그리고 잘못됐을 때: `Esc, Esc`로 즉시 중단합니다.

다음 클립에서는 토큰 절약과 비용 관리 전략을 다룹니다.
