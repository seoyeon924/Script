---
title: CH01-04 슬래시 커맨드 완전 정리
chapter: CH01
clip: 04
time: 10분
type: 슬라이드 + 데모
---

# CH01-04 슬래시 커맨드 완전 정리

## 클립 목표
클로드 코드 인터랙티브 모드에서 자주 쓰는 슬래시 커맨드를 익힙니다.

---

## 1. 슬래시 커맨드란 (1분)

클로드 코드 대화 중에 `/`로 시작하는 명령어를 입력하면, 특정 기능을 즉시 실행합니다.
채팅에서 "이전 내용 요약해줘"라고 말하는 것보다, `/compact`를 입력하는 것이 정확하고 빠릅니다.

---

## 2. 필수 슬래시 커맨드 (6분)

### 컨텍스트 관리

**`/compact`**
대화 내용을 요약해서 컨텍스트 크기를 줄입니다.
대화가 길어지면 토큰 사용량이 늘어납니다. `/compact`를 실행하면 핵심만 남기고 정리합니다.
비유하자면, 회의록을 전체 녹취록 대신 요점 정리로 교체하는 것과 같습니다.

```
/compact
```

**`/clear`**
대화 히스토리를 완전히 초기화합니다.
새 작업을 시작할 때 이전 내용이 방해가 되는 경우 사용합니다.

```
/clear
```

### 작업 안전망

**`/undo`**
마지막 작업을 되돌립니다.
파일 수정 후 결과가 마음에 들지 않을 때 사용합니다.

```
/undo
```

**`/review`**
클로드가 변경한 내용 전체를 diff 형식으로 보여줍니다.
"지금까지 어떤 파일을 어떻게 바꿨는지" 한눈에 확인할 수 있습니다.

```
/review
```

### 설정 및 정보

**`/config`**
설정 화면을 엽니다. 모델 선택, 권한 설정, 자동 승인 설정 등을 변경합니다.

```
/config
```

**`/status`**
현재 세션 상태를 확인합니다. 사용한 토큰 수, 남은 컨텍스트 용량을 확인할 수 있습니다.

```
/status
```

**`/help`**
사용 가능한 슬래시 커맨드 전체 목록을 출력합니다.

```
/help
```

### 서브에이전트 관련

**`/agents`**
현재 사용 가능한 서브에이전트 목록을 확인합니다. 서브에이전트는 다음 클립에서 다룹니다.

```
/agents
```

### 스케줄링 및 원격 제어

**`/loop`**
반복 작업을 스케줄링합니다. 배포 상태 확인, 빌드 모니터링 등에 유용합니다.

```
/loop 5m 배포 완료됐는지 확인해줘
```

시간 단위는 `s`(초), `m`(분), `h`(시간), `d`(일)을 지원합니다.
시간을 생략하면 기본 10분 간격으로 실행됩니다.

스케줄된 작업 목록 확인:
```
어떤 작업이 스케줄되어 있어?
```

특정 작업 취소:
```
배포 체크 작업 취소해줘
```

⚠️ **주의**: 스케줄된 작업은 세션이 열려 있는 동안만 동작합니다. 터미널을 닫으면 모든 스케줄이 사라집니다.

**`/remote-control`** (또는 `/rc`)
현재 세션을 다른 기기에서 이어받을 수 있게 합니다.

```
/remote-control
```

실행하면 세션 URL과 QR 코드가 표시됩니다.
핸드폰이나 다른 브라우저에서 해당 URL로 접속하면 같은 세션을 이어서 사용할 수 있습니다.

로컬 파일시스템, MCP 서버, 프로젝트 설정이 모두 그대로 유지됩니다.
노트북에서 작업하다가 소파에서 핸드폰으로 이어받을 때 유용합니다.

---

## 3. 단축키 (3분)

슬래시 커맨드 외에 키보드 단축키도 있습니다.

| 단축키 | 기능 |
|--------|------|
| `Ctrl+C` | 현재 작업 중단 |
| `Ctrl+D` | 세션 종료 |
| `Ctrl+L` | 터미널 화면 정리 (대화 히스토리는 유지) |
| `Ctrl+R` | 이전 명령어 검색 |
| `Ctrl+O` | 상세 출력 모드 토글 (도구 사용 내역 확인) |
| `Esc, Esc` | 진행 중인 작업 중단 후 이전 상태로 |

`Esc, Esc` (이스케이프 두 번)는 클로드가 작업 도중 잘못된 방향으로 가고 있을 때 즉시 중단하는 가장 빠른 방법입니다.

---

## 직접 해보기

**목표**: 가장 자주 쓰는 슬래시 커맨드 3가지를 직접 실행합니다.

**클로드 코드 세션을 시작합니다**
```bash
claude
```

**1. 사용 가능한 커맨드 목록 확인**
```
/help
```

**2. 현재 세션 상태 확인 (토큰 사용량 포함)**
```
/status
```

**3. 컨텍스트를 초기화하고 새로 시작**
```
/clear
지금부터 새 분석 작업 시작할게. data/campaign.csv 읽어줘.
```

→ `/clear` 후에는 이전 대화 내용을 기억하지 않습니다.
→ 새로운 작업을 시작할 때 컨텍스트를 깨끗하게 만드는 습관을 들이면 토큰 절약이 됩니다.

---

## 핵심 정리

자주 쓰는 슬래시 커맨드 3가지만 기억합니다.

1. `/compact` — 대화가 길어지면 컨텍스트 압축
2. `/clear` — 새 작업 시작 시 초기화
3. `/review` — 변경 내용 확인

그리고 잘못됐을 때: `Esc, Esc`로 즉시 중단합니다.

다음 클립에서는 토큰 절약과 비용 관리 전략을 다룹니다.
