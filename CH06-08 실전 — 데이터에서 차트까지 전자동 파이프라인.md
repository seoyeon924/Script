---
tags: #CH06 #실전 #전자동화 #파이프라인 #시각화완성
time: 14분
chapter: CH06
status: ✅ 대본완성
---

# CH06-08 실전 — 데이터에서 차트까지 전자동 파이프라인

## 📌 클립 정보
- **예상시간:** 14분
- **유형:** 화면 녹화 위주 (실전 실습)
- **준비물:** CH06-01~07 내용 적용된 프로젝트 환경

---

## 전자동 파이프라인의 구조 (3분)

이 클립에서 완성하는 파이프라인 구조입니다.

```
새 씨에스브이 데이터 입력
        ↓
에이전트: 데이터 구조 분석
  - 컬럼 유형 파악 (시간, 카테고리, 수치)
  - 질문 유형 분류 (트렌드/비교/구성/관계)
        ↓
에이전트: 차트 유형 자동 선택
  - CLAUDE.md 시각화 규칙 적용
        ↓
에이전트: 차트 생성
  - 브랜드 색상 자동 적용
  - 인사이트 기반 제목 작성
        ↓
에이전트: 리포트 저장 + (선택) 슬랙 전송
```

한 번 설계하면 데이터만 바꿔도 전체 과정이 자동으로 실행됩니다.

---

## CLAUDE.md 최종 버전 구성 (3분)

전자동 파이프라인을 위한 클로드엠디 최종 구성입니다.

```markdown
# 데이터 시각화 자동화 프로젝트

## 프로젝트 규칙
- data/raw/ 파일 수정 금지
- 모든 결과: data/output/ 저장
- 차트: charts/ 저장
- 리포트: reports/ 저장

## 시각화 자동 선택 기준
- 시간 컬럼(date, month, year 포함) → 라인 차트
- 카테고리 비교 (7개 이하) → 세로 막대 차트
- 카테고리 비교 (8개 이상) → 가로 막대 차트
- 비율/구성 (합이 100%) → 파이 차트
- 수치 × 수치 관계 → 스캐터 플롯
- 그룹별 분포 비교 → 박스 플롯

## 색상 기준
- 강조 1개: #2563EB (진한 파랑)
- 일반: #9CA3AF (연한 회색)
- 긍정: #059669, 경고: #DC2626

## 차트 기본 설정
- 크기: 10×6 인치, 150dpi
- 제목: 인사이트 한 줄 (기술적 설명 금지)
- 라이브러리: 정적=맷플롯립, 인터랙티브=플롯리
```

---

## 실전 실행 — 새 데이터 자동 처리 (5분)

새로운 씨에스브이 파일을 `data/raw/`에 넣고 하나의 지시만 입력합니다.

```bash
claude
```

> "data/raw/ 폴더에 새 씨에스브이 파일이 있어. 이 파일의 구조를 분석해서 CLAUDE.md 시각화 기준에 따라 가장 적합한 차트 3개를 선택하고 자동으로 생성해줘. 각 차트는 charts/ 폴더에 저장하고, 발견한 주요 인사이트 3가지를 reports/auto_report.md에 정리해줘."

에이전트가 처리하는 과정입니다.

첫 번째, 씨에스브이를 읽고 컬럼 구조를 파악합니다.
두 번째, 클로드엠디 기준을 적용해서 차트 유형 3개를 선택합니다.
세 번째, 브랜드 색상이 적용된 차트를 생성합니다.
네 번째, 인사이트를 리포트로 정리합니다.

---

## 주기 자동화 설정 (1분)

매일 또는 매주 자동으로 실행하려면 크론탭을 설정합니다.

```bash
crontab -e
```

```
# 매주 월요일 오전 9시 자동 시각화 실행
0 9 * * 1 cd ~/my-analysis && claude --print "data/raw/ 새 파일 분석해서 차트 3개 생성하고 reports/weekly.md 저장" >> logs/auto.log 2>&1
```

새 데이터 파일을 `data/raw/`에 넣어두면 매주 월요일에 자동으로 차트가 생성됩니다.

---

## 직접 해보기

**목표**: 새 데이터를 넣고 전자동 파이프라인을 실행합니다.

**준비 (터미널에 복붙)**
```bash
cd ~/chart-test

# 새 테스트 데이터 생성
python3 -c "
import pandas as pd, numpy as np
np.random.seed(1)
dates = pd.date_range('2024-01', '2024-04', freq='W').strftime('%Y-%m-%d')
data = []
for d in dates:
    for ch in ['SNS','검색','이메일','디스플레이']:
        s = np.random.randint(200000,800000)
        conv = np.random.randint(30,200)
        data.append({'date':d,'channel':ch,'spend':s,'conversions':conv,'revenue':conv*np.random.randint(15000,40000)})
pd.DataFrame(data).to_csv('data/raw/q1_2024.csv',index=False)
print('새 데이터 준비 완료')
"

claude
```

**클로드에게 이렇게 말합니다**
```
data/raw/q1_2024.csv를 분석해줘.
CLAUDE.md 기준에 따라 가장 적합한 차트 3개를 선택해서 자동 생성해줘.
각 차트는 charts/ 저장, 인사이트 3가지는 reports/q1_report.md에 정리해줘.
```

→ 에이전트가 데이터 구조를 분석하고, 차트 유형을 스스로 선택해서 생성합니다.

---

## 핵심 정리 (30초)

클로드엠디에 시각화 규칙을 정의하면 에이전트가 새 데이터를 받을 때마다 자동으로 적합한 차트를 선택하고 생성합니다.
한 번 설계한 파이프라인은 데이터만 바꾸면 계속 재사용합니다.
크론탭으로 주기를 설정하면 사람의 개입 없이 반복 실행됩니다.

## 챕터 마무리 (30초)

이것으로 CH06 시각화 전략 자동 설계를 마칩니다.
트렌드 분석, 차트 유형 선택 기준부터 파이썬 자동화, 인터랙티브 차트, 브랜드 일관성까지 전체 흐름을 다루었습니다.
다음 챕터에서는 디쓰리제이에스를 활용한 커스텀 인터랙티브 시각화를 다룹니다.
