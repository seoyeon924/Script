---
tags: #CH08 #Antigravity #워크플로우
time: 18분
chapter: CH08
status: ✅ 대본완성
---

# CH08-02 데이터에서 차트까지 — Antigravity 기본 워크플로우

## 📌 클립 정보
- **예상시간:** 18분
- **유형:** 화면 녹화 중심
- **준비물:** CH08-01 선행 필수, 샘플 데이터

---

## 학습 목표 선언 (30초)

이 클립을 마치면 두 가지를 할 수 있습니다.

첫 번째, Antigravity로 씨에스브이 데이터를 차트로 변환할 수 있습니다.
두 번째, 차트 타입과 스타일을 지정하는 방법을 이해합니다.

---

## 샘플 데이터 준비 (3분)

**[화면: 터미널 + 데이터 미리보기]**

실습용 판매 데이터를 준비합니다.

```bash
cd ~/antigravity-test
mkdir data
```

샘플 데이터 구조입니다:

```csv
월,채널,매출,전환율
1월,온라인,45000000,3.2
1월,오프라인,32000000,5.1
2월,온라인,48000000,3.5
2월,오프라인,31000000,4.8
3월,온라인,52000000,3.8
3월,오프라인,33000000,5.3
```

---

## 첫 번째 차트 생성 (5분)

**[화면: 터미널 + 브라우저]**

Antigravity로 막대 차트를 생성합니다.

```bash
antigravity chart bar \
  --data data/sales.csv \
  --x 월 \
  --y 매출 \
  --group 채널 \
  --output output/sales-bar.html
```

결과를 브라우저에서 확인합니다.

```bash
open output/sales-bar.html
```

자동으로 생성된 차트가 나타납니다.
채널별로 색상이 구분되고, 범례도 자동 생성됩니다.

---

## Claude Code와 연동하기 (6분)

**[화면: 터미널 — Claude Code 실행]**

이제 Claude Code에게 전체 과정을 맡겨봅니다.

```bash
claude
```

**클로드에게 이렇게 말합니다:**
```
data/sales.csv 파일을 분석해서 채널별 매출 추이를 보여주는 
막대 차트를 Antigravity로 만들어줘. 
차트 제목은 "2024 상반기 채널별 매출 현황"으로 하고,
output/ 폴더에 저장해줘.
```

Claude Code가 자동으로:
1. 데이터 구조를 파악합니다
2. 적절한 차트 타입을 선택합니다
3. Antigravity 명령어를 실행합니다
4. 결과 파일을 저장합니다

---

## 차트 타입 변경하기 (3분)

**[화면: 다양한 차트 예시]**

Antigravity가 지원하는 차트 타입입니다:

| 타입 | 명령어 | 적합한 데이터 |
|------|--------|---------------|
| 막대 | `chart bar` | 범주 비교 |
| 라인 | `chart line` | 시계열 추이 |
| 파이 | `chart pie` | 구성비 |
| 스캐터 | `chart scatter` | 상관관계 |
| 히트맵 | `chart heatmap` | 2차원 분포 |

**클로드에게 차트 타입 변경 요청:**
```
같은 데이터로 라인 차트도 만들어줘. 
월별 매출 추이를 보기 좋게.
```

---

## 직접 해보기

**목표**: Claude Code로 데이터 분석 + Antigravity 시각화를 한 번에 실행합니다.

**준비 (터미널에 복붙)**
```bash
cd ~/antigravity-test
claude
```

**클로드에게 이렇게 말합니다**
```
data/sales.csv에서 채널별 평균 전환율을 계산하고,
결과를 막대 차트로 시각화해줘.
Antigravity를 사용하고 output/conversion-rate.html로 저장해.
```

→ Claude Code가 분석 + 시각화를 자동 처리합니다.

**확인하기**
```bash
open output/conversion-rate.html
```

## 핵심 정리 (1분)

Antigravity로 씨에스브이 데이터를 간단한 명령어로 차트로 변환할 수 있습니다.
Claude Code와 연동하면 "분석해서 시각화해줘"라는 한 문장으로 전체 과정이 자동화됩니다.

## 다음 클립 예고 (30초)

다음 클립에서는 차트 스타일을 커스터마이징하고 브랜드 가이드를 적용하는 방법을 다룹니다.
