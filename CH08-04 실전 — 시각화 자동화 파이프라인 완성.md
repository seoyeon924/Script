---
tags: #CH08 #안티그래비티 #실전 #파이프라인 #시각화자동화
time: 17분
chapter: CH08
status: ✅ 대본완성
---

# CH08-04 실전 — 시각화 자동화 파이프라인 완성

## 📌 클립 정보
- **예상시간:** 17분
- **유형:** 화면 녹화 (실전 실습)
- **준비물:** 안티그래비티 + 클로드 코드 연결 완료, 샘플 데이터

---

## 학습 목표 선언 (30초)

이 클립을 마치면 두 가지를 할 수 있습니다.

첫 번째, 안티그래비티 환경에서 8단계 파이프라인을 처음부터 끝까지 실행할 수 있습니다.
두 번째, 에디터로 결과를 확인하면서 최소한의 토큰으로 분석을 완성하는 흐름을 경험할 수 있습니다.

---

## 실전 시나리오 (2분)

마케팅팀 월간 성과 분석을 시나리오로 진행합니다.

**상황:**
월말에 광고 성과 씨에스브이가 data/raw/ 폴더에 들어옵니다.
8단계 파이프라인을 실행합니다.
최종 대시보드와 보고서를 완성합니다.

**목표:**
클로드에게 지시는 최소화하고, 에디터로 직접 확인하면서 토큰을 아낍니다.

---

## 단계별 실행 (10분)

**[화면: 안티그래비티 분할 레이아웃]**

**STEP 1 — 파이프라인 시작 지시**

터미널에서 클로드 코드를 실행하고 전체 파이프라인을 한 번에 지시합니다.

```
data/raw/ 에 있는 campaigns.csv로 8단계 파이프라인 실행해줘.
문제 정의: 채널별 광고 효율성 분석 및 다음 달 예산 최적화 추천.
CLAUDE.md 파이프라인 규칙 따라줘.
```

**STEP 2 — 에디터로 전처리 결과 확인**

클로드가 전처리를 마치면 에디터에서 직접 확인합니다.
`data/processed/cleaned_data.csv` 열어서 이상 없는지 봅니다.
클로드에게 "전처리 결과 보여줘" 라고 물어보지 않습니다.

**STEP 3 — 인사이트 확인 후 피드백**

에디터에서 `data/output/insights.md` 를 확인합니다.
내용이 충분하면 다음 단계를 지시합니다.
내용이 부족하면 추가 분석을 요청합니다.

```
insights.md 봤는데 SNS 채널 분석이 더 자세하면 좋겠어.
SNS만 일별로 쪼개서 분석해줘.
```

**STEP 4 — 차트 확인 후 수정 지시**

에디터에서 차트 코드를 직접 확인합니다.
색상이 마음에 안 들면 에디터에서 직접 수정합니다.
크기나 레이아웃은 클로드에게 맡깁니다.

**STEP 5 — 대시보드 최종 확인**

브라우저에서 `charts/dashboard.html`을 열어서 확인합니다.
수정이 필요한 부분을 에디터에서 직접 또는 클로드에게 지시합니다.

---

## 토큰 사용 비교 (3분)

같은 작업을 안티그래비티 없이 했을 때와 비교합니다.

**안티그래비티 없이 (터미널만):**
파일 확인 요청 × 10 → 약 5,000 토큰 추가 사용
결과 출력 요청 × 5 → 약 3,000 토큰 추가 사용

**안티그래비티와 함께:**
파일 확인: 에디터로 직접 → 0 토큰
결과 출력: 에디터로 직접 → 0 토큰

하루 작업 기준 약 20~30% 토큰 절약 효과가 있습니다.
클로드 프로 한도 내에서 더 긴 작업을 처리할 수 있습니다.

---

## 직접 해보기

**목표**: 안티그래비티에서 전체 파이프라인을 실행합니다.

**준비 (안티그래비티 터미널에서)**
```bash
mkdir ~/pipeline-ag && cd ~/pipeline-ag
mkdir -p data/raw data/processed data/output charts reports scripts
python3 -c "
import pandas as pd, numpy as np, random
random.seed(42)
dates = pd.date_range('2024-01', '2024-04', freq='W')
rows = []
for d in dates:
    for ch in ['SNS','검색','이메일','디스플레이']:
        s = random.randint(200000,800000)
        c = random.randint(30,200)
        rows.append({'date':str(d.date()),'channel':ch,'spend':s,'conversions':c,'revenue':c*random.randint(15000,40000)})
pd.DataFrame(rows).to_csv('data/raw/campaigns.csv',index=False)
print('완료')
"
claude
```

**클로드에게 이렇게 말합니다**
```
data/raw/campaigns.csv로 8단계 파이프라인 실행해줘.
문제 정의: "채널별 광고 효율성 분석 및 예산 최적화"
대시보드: charts/dashboard.html
보고서: reports/final_report.md
```

→ 각 단계 완료 후 에디터로 직접 확인하면서 진행합니다.

---

## 챕터 마무리 (30초)

이것으로 CH08 클로드 코드 × 안티그래비티를 마칩니다.
다음 챕터부터는 마케팅, 피엠, 에이치알 등 실제 직무 데이터를 활용한 프로젝트 챕터가 시작됩니다.
