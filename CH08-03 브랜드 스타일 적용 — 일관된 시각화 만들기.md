---
tags: #CH08 #Antigravity #브랜드스타일
time: 15분
chapter: CH08
status: ✅ 대본완성
---

# CH08-03 브랜드 스타일 적용 — 일관된 시각화 만들기

## 📌 클립 정보
- **예상시간:** 15분
- **유형:** 화면 녹화 + 슬라이드
- **준비물:** CH08-02 선행 필수

---

## 학습 목표 선언 (30초)

이 클립을 마치면 두 가지를 할 수 있습니다.

첫 번째, Antigravity 테마 파일을 만들어서 브랜드 스타일을 적용할 수 있습니다.
두 번째, 모든 차트에 일관된 색상과 폰트를 자동 적용할 수 있습니다.

---

## 왜 브랜드 스타일이 중요한가 (3분)

**[화면: 일관성 있는 차트 vs 없는 차트]**

같은 데이터라도 스타일이 다르면 전문성이 떨어져 보입니다.

**일관성 없는 경우:**
- 차트마다 다른 색상
- 폰트가 제각각
- 그래프 스타일 불일치

**브랜드 스타일 적용 후:**
- 회사 색상 일관 적용
- 통일된 폰트
- 전문적인 인상

Antigravity 테마 파일을 사용하면 한 번 설정으로 모든 차트에 자동 적용됩니다.

---

## 테마 파일 만들기 (5분)

**[화면: 에디터 — 테마 파일 작성]**

`themes/company.json` 파일을 생성합니다:

```json
{
  "name": "company-brand",
  "colors": {
    "primary": "#2563EB",
    "secondary": "#10B981",
    "accent": "#F59E0B",
    "background": "#FFFFFF",
    "text": "#1F2937"
  },
  "palette": [
    "#2563EB",
    "#10B981",
    "#F59E0B",
    "#EF4444",
    "#8B5CF6"
  ],
  "font": {
    "family": "Pretendard, sans-serif",
    "titleSize": 18,
    "labelSize": 12
  },
  "chart": {
    "borderRadius": 4,
    "gridColor": "#E5E7EB",
    "animation": true
  }
}
```

이 파일 하나로 모든 차트의 스타일이 통일됩니다.

---

## 테마 적용하기 (4분)

**[화면: 터미널]**

테마를 적용해서 차트를 생성합니다:

```bash
antigravity chart bar \
  --data data/sales.csv \
  --x 월 \
  --y 매출 \
  --theme themes/company.json \
  --output output/branded-chart.html
```

또는 기본 테마로 설정:

```bash
antigravity config set theme themes/company.json
```

이제 모든 차트가 자동으로 브랜드 스타일을 사용합니다.

---

## Claude Code로 테마 자동 생성 (4분)

**[화면: Claude Code 실행]**

Claude Code에게 테마 생성을 요청합니다:

**클로드에게 이렇게 말합니다:**
```
우리 회사 브랜드 색상이 메인 #1E40AF, 서브 #059669야.
이 색상을 기반으로 Antigravity 테마 파일을 만들어줘.
색맹 친화적인 팔레트로 확장하고,
themes/databridge.json으로 저장해줘.
```

Claude Code가 자동으로:
1. 메인 색상에서 조화로운 팔레트를 생성합니다
2. 색맹 친화적인 색상 조합을 선택합니다
3. JSON 테마 파일을 생성합니다

---

## 직접 해보기

**목표**: 나만의 브랜드 테마를 만들고 차트에 적용합니다.

**터미널에서 실행**
```bash
cd ~/antigravity-test
mkdir themes
claude
```

**클로드에게 이렇게 말합니다**
```
내가 좋아하는 색상 #6366F1(보라)와 #EC4899(핑크)를 기반으로
Antigravity 테마를 만들어줘.
전문적이면서 세련된 느낌으로.
themes/my-brand.json으로 저장해.
```

**테마 적용해서 차트 생성**
```
이 테마를 적용해서 data/sales.csv로 막대 차트 만들어줘.
```

## 핵심 정리 (1분)

Antigravity 테마 파일로 브랜드 스타일을 정의합니다.
한 번 설정하면 모든 차트에 자동 적용됩니다.
Claude Code로 색상 팔레트와 테마 파일을 자동 생성할 수 있습니다.

## 다음 클립 예고 (30초)

다음 클립에서는 생성한 차트를 웹에 자동 배포하는 방법을 다룹니다.
