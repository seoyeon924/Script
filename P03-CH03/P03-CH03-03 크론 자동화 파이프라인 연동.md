---
tags: #P03 #CH03 #OpenClaw #크론 #파이프라인 #자동화
time: 15분
part: Part 03
chapter: CH03
clip: 03
slides: s11,s12,s13,s23,s14,s15,s25,s29
status: ✅ 대본완성
---

## 🎬 CLIP 03 촬영 가이드
**슬라이드 순서:** s11 → s12 → s13 → s23 → s14 → s15 → s25 → s29

#### s11~s12 — 슬라이드만

---

#### s13 — 크론 스케줄 확인 `[터미널]`

```bash
openclaw cron list
```

---

#### s23 — 크론 message 등록 `[데모 — 터미널]`

복붙용:
```bash
# 매주 월요일 오전 9시
openclaw cron add \
  --schedule "0 9 * * 1" \
  --message "~/Projects/openclaw-pipeline-kit/runs/latest/input/ 에 있는 CSV 파일로 /run-pipeline 을 실행하세요. 완료 후 생성된 파일 목록을 알려주세요." \
  --agent main

# 매일 오전 8시
openclaw cron add \
  --every 1d \
  --message "~/Projects/openclaw-pipeline-kit/runs/latest/input/ 최신 파일 분석해서 핵심 지표 3개와 전일 대비 변화 요약해줘." \
  --agent main
```

---

#### s14 — /run-pipeline 실행 `[데모 — 텔레그램 + 터미널]`

**열 것:** 텔레그램 Echo 봇 DM + 터미널

텔레그램 복붙:
```
~/Projects/openclaw-pipeline-kit/runs/latest/input/ 에 있는 CSV 파일로
파이프라인 실행해줘.
완료되면 생성된 파일 목록 알려줘.
```

로그 확인:
```bash
openclaw logs --agent main --follow
```

---

#### s15 — 결과 수신 `[텔레그램 화면 보여주기]`

파이프라인 완료 후 텔레그램으로 자동 수신되는 메시지 보여주기.
미리 돌려둔 결과 화면 캡처 또는 녹화본 사용 권장.

---

#### s25 — 에이전트 팀 협업 `[데모 — 텔레그램]`

텔레그램 Echo 봇 DM 복붙:
```
분석 작업 들어오면 이 순서로 처리해줘.
1. 네가 먼저 EDA + 문제 정의
2. Min 호출 → 지표 설계 + 대시보드 피드백
3. Evan 호출 → 대시보드 구현 + 마무리
이 순서를 AGENTS.md에 기본 규칙으로 써줘.
```

---

#### s29 — 파이프라인 전체 실행 라이브 데모 `[핵심 데모]`

**열 것:** 터미널 + 텔레그램 + 브라우저(activity-monitor)

**Step 1.** CSV 확인
```bash
ls ~/Projects/openclaw-pipeline-kit/runs/latest/input/
```

**Step 2.** 텔레그램 Echo 봇에 전송 (복붙):
```
~/Projects/openclaw-pipeline-kit/runs/latest/input/ 에 CSV 있어.
파이프라인 실행해줘.
완료되면 생성된 파일 목록 알려줘.
```

**Step 3.** `localhost:9001/activity-monitor.html` — 10초마다 자동 갱신, 스테이지 ✓ 확인

**Step 4.** 완료 후 결과 확인
```bash
ls ~/Projects/openclaw-pipeline-kit/runs/latest/outputs/
open ~/Projects/openclaw-pipeline-kit/runs/latest/outputs/06_dashboard.html
```

---

## s11 — 조감도

Clip 01 연결, Clip 02 에이전트 만들기까지 했어요. 이번 클립에서는 크론 스케줄로 완전 자동화를 연결해요. 정해진 시간이 되면 에이전트가 알아서 실행하고, 결과를 텔레그램으로 보내요.

---

## s12 — CLIP 03 섹션 브레이크

크론 자동화 파이프라인 연동이에요. 스케줄 설정하면 에이전트가 자는 동안 자동 실행돼요.

---

## s13 — 크론 스케줄 종류

**[화면: 터미널]**

크론 스케줄 방식이 두 가지예요.

`--every`는 간단한 주기예요. `1d`면 매일, `2h`면 2시간마다예요.

`--schedule`은 cron 표현식이에요. `0 9 * * 1`이면 매주 월요일 오전 9시예요. 시간을 정확하게 지정하고 싶을 때 써요.

크론이 에이전트에게 보내는 `message` 필드가 핵심이에요. 사람이 직접 보내는 것과 완전히 같은 형식이에요.

---

## s23 — 크론 message — 바로 쓸 수 있는 프롬프트

**[화면: 터미널]**

프롬프트 작성 팁 세 가지예요.

파일 경로를 절대경로로 명시해요. 상대경로는 실패 가능성 있어요.

완료 조건을 명시해요. "완료 후 파일 목록 출력"처럼 끝내는 조건이 없으면 에이전트가 안 멈출 수 있어요.

슬래시 명령어를 직접 넣어요. `/run-pipeline` 같은 명령어를 프롬프트 안에 직접 넣으면 돼요.

---

## s14 — /run-pipeline 자동 실행 연동

**[화면: 텔레그램 + 터미널]**

이전 파트에서 만든 ai-pipeline-kit을 OpenClaw 크론에 연결하는 거예요.

크론이 매주 월요일 오전 9시에 Echo에게 메시지를 자동 전송해요. Echo가 받아서 `/run-pipeline`을 실행하고, 완료되면 결과를 텔레그램으로 돌려보내요.

사람이 직접 보내는 것과 완전히 같은 형식이에요. 크론이 정해진 시간에 자동으로 전송할 뿐이에요.

---

## s15 — 텔레그램 결과 전송 & delivery

**[화면: 텔레그램]**

파이프라인이 완료되면 텔레그램으로 자동 알림이 와요.

흐름은 세 단계예요. 크론 시간이 되면 OpenClaw가 에이전트를 깨워서 메시지를 전달해요. 에이전트가 작업을 완료하면 텔레그램으로 결과를 자동 전송해요. 컴퓨터 앞에 있을 필요 없이 텔레그램으로 결과를 수신하면 돼요.

작업은 에이전트가, 결과는 텔레그램으로 수신이에요.

---

## s25 — 에이전트 팀 협업 — 역할 분담 자동화

**[화면: 텔레그램]**

에이전트 한 명이 아니라 팀으로 일하게 할 수 있어요.

Echo에게 순서를 AGENTS.md에 써달라고 하면 돼요. Echo가 먼저 EDA와 문제 정의를 하고, Min을 호출해서 지표 설계와 피드백을 받고, Evan을 호출해서 대시보드를 만들게 해요.

장점은 역할별 전문화랑 메모리 독립이에요. 주의할 점은 토큰이 2~3배 나와요. Min, Evan은 Haiku 모델로 설정하면 비용 절감이 돼요.

---

## s29 — 실전 실행 — CSV 넣고 텔레그램으로 시작하기

**[화면: 터미널 → 텔레그램 → 브라우저]**

실제로 어떻게 돌아가는지 흐름을 정리할게요.

에이전트가 5명이에요. Echo가 팀장이고 나머지 4명이 순서대로 일해요.

CSV 파일을 `runs/latest/input/` 폴더에 넣어요. 어떤 CSV든 상관없어요.

텔레그램 Echo 봇에 "runs/latest/input/ 에 CSV 있어. 파이프라인 실행해줘." 보내면 돼요. 이게 전부예요. 나머지는 에이전트가 알아서 해요.

Aria가 데이터 프로파일이랑 EDA를 하고, Sam이 문제 정의랑 KPI를 뽑고, Min이 분석 리포트를 쓰고, Evan이 대시보드랑 경영진 보고서를 만들어요. 각자 파일로 저장해요.

브라우저에서 `activity-monitor.html` 열어두면 10초마다 자동 갱신되면서 완료된 스테이지가 ✓로 바뀌어요.

다 끝나면 텔레그램으로 결과 파일 목록이 자동으로 와요.

CSV 하나 넣고 메시지 한 줄 보내면 보고서까지 자동 완성이에요.
