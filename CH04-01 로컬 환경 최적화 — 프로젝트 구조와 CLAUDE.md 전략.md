---
tags: #CH04 #로컬환경 #프로젝트구조 #CLAUDE.md #환경설정
time: 13분
chapter: CH04
status: ✅ 대본완성
---

# CH04-01 로컬 환경 최적화 — 프로젝트 구조와 CLAUDE.md 전략

## 📌 클립 정보
- **예상시간:** 13분
- **유형:** 슬라이드 3장 + 화면 녹화
- **준비물:** 클로드 코드 설치 완료, 터미널 준비

---

## 오프닝 (30초)

CH03까지 에이전트 혼자 일하는 것, 여럿이 협력하는 것까지 배웠습니다.
이번 챕터는 환경 얘기입니다. 어떻게 세팅하느냐에 따라 같은 에이전트도 결과가 달라집니다.

---

## 왜 로컬 환경 설계가 중요한가 (3분)

클로드 코드는 프로젝트 폴더를 기준으로 작동합니다.
에이전트가 어디서 시작하느냐, 어떤 파일을 먼저 읽느냐에 따라 결과 품질이 달라집니다.

책상이 정리된 직원과 그렇지 않은 직원을 생각해보세요.
같은 업무 지시를 받아도 결과물이 다르죠. 클로드 코드도 똑같습니다.
클로드 코드도 마찬가지입니다.
폴더 구조가 명확하고 클로드엠디가 잘 작성되어 있으면 에이전트가 더 좋은 결과를 냅니다.

세 가지 이유가 있습니다.

첫 번째, 에이전트가 파일을 찾는 데 쓰는 시간이 줄어듭니다.
두 번째, 클로드엠디를 통해 프로젝트 맥락을 한 번에 전달할 수 있습니다.
세 번째, 여러 세션에 걸쳐 작업할 때 일관성이 유지됩니다.

---

## 데이터 분석 프로젝트 표준 구조 (4분)

데이터 분석 프로젝트에 권장하는 폴더 구조를 소개합니다.

```
my-analysis/
├── CLAUDE.md           ← 에이전트 지시 파일 (필수)
├── data/
│   ├── raw/            ← 원본 데이터 (절대 수정 금지)
│   ├── processed/      ← 전처리된 데이터
│   └── output/         ← 분석 결과물
├── scripts/            ← 파이썬 또는 에스큐엘 스크립트
├── charts/             ← 시각화 결과물
├── reports/            ← 리포트 마크다운/에이치티엠엘
└── .claude/
    └── agents/         ← 서브에이전트 정의 파일
```

이 구조의 핵심 원칙은 세 가지입니다.

첫 번째, `raw/`는 읽기 전용입니다.
원본 데이터는 절대 수정하지 않습니다.
에이전트에게도 이 규칙을 클로드엠디에 명시합니다.

두 번째, 결과물은 항목별로 분리합니다.
스크립트, 차트, 리포트가 섞이면 에이전트가 혼동합니다.

세 번째, `.claude/agents/` 폴더로 서브에이전트를 정의합니다.
CH02에서 배운 서브에이전트 설계와 연결됩니다.

---

## 프로젝트 수준 CLAUDE.md 작성 방법 (4분)

클로드엠디는 에이전트가 프로젝트를 시작할 때 가장 먼저 읽는 파일입니다.

좋은 클로드엠디에는 다섯 가지 항목이 있습니다.

**1. 프로젝트 개요**
이 프로젝트가 무엇을 하는 프로젝트인지 두 문장으로 설명합니다.

**2. 데이터 구조 설명**
주요 씨에스브이 파일의 컬럼 목록과 의미를 작성합니다.
에이전트가 매번 파일을 열어서 확인하는 시간을 줄입니다.

**3. 작업 규칙**
예: `raw/` 폴더는 읽기 전용입니다.
예: 결과 파일은 반드시 `output/` 폴더에 저장합니다.
예: 모든 숫자는 소수점 둘째 자리까지 표시합니다.

**4. 출력 형식**
리포트를 마크다운으로 작성할지, 에이치티엠엘로 작성할지 명시합니다.
차트는 어떤 형식으로 저장할지 명시합니다.

**5. 금지 사항**
예: 원본 데이터 파일 수정 금지.
예: `scripts/` 외부에 파이썬 파일 생성 금지.

실제 예시입니다.

```markdown
# 마케팅 성과 분석 프로젝트

## 프로젝트 개요
2024년 1분기 채널별 광고 성과 데이터를 분석합니다.
목표는 채널별 알오아이 비교와 최적 예산 배분 추천입니다.

## 데이터 구조
- data/raw/campaigns.csv: date, channel, spend, clicks, conversions, revenue
- data/raw/budgets.csv: channel, monthly_budget, q1_total

## 작업 규칙
- raw/ 폴더 파일은 수정하지 않습니다
- 모든 결과는 data/output/ 에 저장합니다
- 금액 단위는 원화, 소수점 없음

## 출력 형식
- 분석 결과: 마크다운 테이블
- 차트: PNG, 800x500 픽셀
```

---

## 화면 실습 — 프로젝트 구조 자동 생성 (1분)

클로드 코드에게 프로젝트 구조 생성을 맡길 수 있습니다.

터미널에서 이렇게 실행합니다.

```bash
mkdir ~/my-analysis && cd ~/my-analysis
claude
```

그리고 이렇게 지시합니다.

> "마케팅 데이터 분석 프로젝트 구조를 만들어줘. data/raw, data/processed, data/output, scripts, charts, reports 폴더 생성하고, CLAUDE.md 파일에 프로젝트 개요, 데이터 규칙, 출력 형식을 포함한 템플릿을 작성해줘."

클로드 코드가 폴더 구조를 생성하고 클로드엠디 초안을 작성합니다.
초안을 검토하고 실제 데이터에 맞게 수정합니다.

---

## 직접 해보기

**목표**: 프로젝트 구조를 생성하고 CLAUDE.md를 작성합니다.

**준비 (터미널에 복붙)**
```bash
mkdir ~/analysis-project && cd ~/analysis-project
claude
```

**클로드에게 이렇게 말합니다**
```
데이터 분석 프로젝트 표준 구조를 만들어줘.
폴더: data/raw, data/processed, data/output, scripts, charts, reports, .claude/agents
CLAUDE.md 파일도 작성해줘. 내용은:
- 프로젝트명: 월별 매출 분석
- raw 폴더 수정 금지
- 결과는 data/output 저장
- 리포트는 마크다운 형식
```

→ 폴더 구조와 CLAUDE.md가 자동으로 생성됩니다.

---

## GitHub 연동 — 로컬 작업을 리포지토리에 저장 (4분)

**[화면: 터미널 + GitHub]**

클로드 코드에서 작업한 내용을 깃허브에 저장하는 방법입니다.
클로드 코드는 터미널 명령어를 실행할 수 있으므로 git 명령어를 직접 사용합니다.

**방법 1: 클로드에게 직접 요청**

```
이 프로젝트를 깃허브에 올려줘.
리포지토리 이름은 marketing-analysis로 해줘.
```

클로드가 다음 순서로 처리합니다.
1. `git init`으로 로컬 저장소 초기화
2. `.gitignore` 파일 생성 (데이터 파일 제외)
3. `git add .`로 파일 스테이징
4. `git commit -m "Initial commit"`으로 커밋
5. `gh repo create`로 깃허브 리포지토리 생성 (GitHub CLI 필요)
6. `git push`로 푸시

**방법 2: 단계별 요청**

```
git 저장소를 초기화해줘
```

```
현재 변경 사항을 커밋해줘. 메시지는 "데이터 전처리 완료"로
```

```
깃허브에 푸시해줘
```

**GitHub CLI(gh) 설치 확인**

깃허브에 리포지토리를 생성하려면 GitHub CLI가 필요합니다.

```bash
# 설치 확인
gh --version

# 없으면 설치 (맥)
brew install gh

# 로그인
gh auth login
```

**주의사항**

- `data/raw/` 폴더의 대용량 데이터는 `.gitignore`에 추가하세요
- API 키나 민감 정보가 포함된 파일은 커밋하지 마세요
- 클로드에게 "민감 정보 제외하고 커밋해줘"라고 지시할 수 있습니다

---

## 핵심 정리 (30초)

로컬 환경 최적화의 핵심은 명확한 폴더 구조와 잘 작성된 클로드엠디입니다.
에이전트는 클로드엠디를 가장 먼저 읽고 프로젝트 맥락을 파악합니다.
프로젝트를 시작할 때 5분 투자해서 구조를 잡으면, 이후 작업 효율이 크게 높아집니다.

## 다음 클립 예고 (30초)

다음 클립에서는 깃허브 코드스페이스를 활용한 클라우드 환경 설정을 다룹니다.
로컬 컴퓨터가 아닌 클라우드에서 에이전트를 실행하면 어떤 장점이 있는지 살펴보겠습니다.
