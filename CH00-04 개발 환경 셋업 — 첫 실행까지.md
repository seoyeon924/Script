---
tags: #CH00 #환경셋업 #설치
time: 20분
chapter: CH00
status: ✅ 대본완성
---

# CH00-04 개발 환경 셋업 — 첫 실행까지

## 📌 클립 정보
- **예상시간:** 20분
- **유형:** 화면 녹화 (터미널 + 브라우저)
- **준비물:** 맥 또는 윈도우 컴퓨터, 인터넷 연결, 클로드 닷에이아이 계정

---

## 오프닝 (1분)

이 클립은 클로드 코드를 처음 실행하기까지 필요한 모든 셋업을 다룹니다.

세 단계로 진행합니다.
첫 번째로 클로드 닷에이아이 구독을 확인합니다.
두 번째로 클로드 코드를 설치합니다.
세 번째로 로그인 후 첫 실행을 확인합니다.

터미널을 처음 쓰는 분도 화면 그대로 따라오면 됩니다.

**노드제이에스는 별도로 설치하지 않아도 됩니다.**
클로드 코드는 네이티브 설치 방식을 사용합니다.

---

## 1단계 — 구독 플랜 확인 (3분)

**[화면: claude.com/pricing 접속]**

클로드 코드를 사용하려면 클로드 닷에이아이 구독이 필요합니다.

| 플랜 | 가격 | 추천 대상 |
|------|------|---------|
| Pro | $20/월 | 처음 써보고 싶은 분 |
| Max (5x) | $100/월 | 본격적으로 쓰고 싶은 분 |
| Max (20x) | $200/월 | 헤비유저, 대규모 프로젝트 |

본격적으로 클로드 코드를 사용하려면 맥스 100달러 플랜을 권장합니다.
프로 플랜은 사용량 한도가 빠르게 차서 집중적으로 사용하기 어렵습니다.

---

## 2단계 — 클로드 코드 설치 (5분)

**[화면: 터미널 실행]**

터미널을 엽니다.
맥에서는 스팟라이트(커맨드 + 스페이스)에서 "터미널"을 검색하면 됩니다.
윈도우에서는 윈도우 키를 누르고 "powershell"을 입력합니다.

**[화면: 설치 명령어 입력]**

맥 또는 리눅스에서는 이 명령어를 입력합니다.

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

맥에서 홈브루를 사용한다면 이 방법도 있습니다.

```bash
brew install --cask claude-code
```

윈도우 파워쉘에서는 이 명령어를 입력합니다.

```powershell
irm https://claude.ai/install.ps1 | iex
```

설치가 끝나면 확인합니다.

```bash
claude --version
```

버전 번호가 표시되면 설치가 정상입니다.

---

## 설치가 안 될 때 — npm 대안 방법

위 방법이 실패하는 경우가 있습니다.
**회사 맥북처럼 보안 정책(엠디엠)이 있는 환경**에서 주로 발생합니다.

원인은 두 가지입니다.
첫 번째, 회사 보안 정책이 외부 스크립트(`curl | bash`) 실행을 차단하는 경우입니다.
두 번째, `~/.local/bin/` 폴더에 파일을 쓰는 권한이 제한된 경우입니다.

이럴 때는 npm 방식을 사용합니다.
공식 문서에서 deprecated(구식) 방법으로 분류하지만, 회사 맥에서는 현실적인 대안입니다.

**npm 방식은 Node.js 18 이상이 필요합니다.**

```bash
node -v
```

버전이 18 미만이거나 없으면 nodejs.org에서 엘티에스 버전을 설치합니다.

그 다음 클로드 코드를 설치합니다.

```bash
npm install -g @anthropic-ai/claude-code
```

> ⚠️ `sudo npm install -g`는 사용하지 마세요.

설치 확인:
```bash
claude --version
```

---

## 3단계 — 로그인 (3분)

**[화면: 터미널에서 claude 실행]**

설치 후 처음 실행하면 브라우저가 자동으로 열립니다.

```bash
claude
```

브라우저에서 클로드 닷에이아이 계정으로 로그인합니다.
오이오스(OAuth) 방식으로 인증이 자동 완료됩니다.

에이피아이 키를 별도로 발급하거나 환경 변수로 등록할 필요가 없습니다.

터미널로 자동 복귀하면서 인증 완료 메시지가 표시됩니다.

---

## 4단계 — 첫 실행 확인 (5분)

**[화면: 터미널]**

```bash
mkdir test-session
cd test-session
claude
```

프롬프트가 표시되면 정상 실행된 것입니다.

다음을 입력합니다.

```
이 폴더에 hello.txt 파일 만들어줘. 내용은 "클로드 코드 시작"
```

클로드 코드가 어떤 도구를 사용하는지, 어떤 파일을 생성하는지 터미널에 표시됩니다.
결과를 보여주는 것이 아니라 과정이 투명하게 보이는 것이 클로드 코드의 특징입니다.

세션을 종료할 때는 `/exit`를 입력하거나 컨트롤+디를 누릅니다.

---

## 마무리 (1분)

설치와 첫 실행을 완료했습니다.

클로드 닷에이아이 계정 로그인, 클로드 코드 설치, 첫 실행 확인까지 모두 마쳤습니다.

이제 씨에이치 영일로 넘어갑니다.
씨에이치 영일에서는 클로드 코드가 어떤 구조로 동작하는지, 에이전트가 무엇인지부터 시작합니다.

---

## 🗒️ 서연 메모
- [ ] 터미널 폰트 크기 키우기 (화면 녹화 전, 18pt 이상)
- [ ] 맥/윈도우 각각 화면 분리 녹화 또는 자막 처리
- [ ] 브라우저 OAuth 로그인 화면 시연 캡처
- [ ] 첫 실행 시 hello.txt 생성 결과 확인 화면 캡처
