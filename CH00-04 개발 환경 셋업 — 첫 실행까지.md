---
tags: #CH00 #환경셋업 #설치
time: 20분
chapter: CH00
status: ✅ 대본완성
---

# CH00-04 개발 환경 셋업 — 첫 실행까지

## 📌 클립 정보
- **예상시간:** 20분
- **유형:** 화면 녹화 (터미널 + 브라우저)
- **준비물:** 맥 또는 윈도우 컴퓨터, 인터넷 연결

---

## 오프닝 (1분)

> 📎 **공식 설치 가이드**
> - [Claude Code 설치 페이지](https://claude.ai/code) — 원클릭 설치
> - [Claude Code 문서](https://docs.anthropic.com/en/docs/claude-code/getting-started) — 시작 가이드
> - [Google Antigravity](https://antigravity.google) — 코드 에디터 + 에이전트 매니저 (맥 애플실리콘)

이 클립은 클로드 코드를 처음 실행하기까지 필요한 모든 셋업을 다룹니다.

세 단계로 진행합니다.
첫 번째로 노드제이에스를 확인하고 설치합니다.
두 번째로 앤스로픽 에이피아이 키를 발급합니다.
세 번째로 클로드 코드를 설치하고 첫 실행을 확인합니다.

터미널을 처음 쓰는 분도 화면 그대로 따라오면 됩니다.

---

## 1단계 — 노드제이에스 확인 (4분)

**[화면: 터미널 실행]**

터미널을 엽니다.
맥에서는 스팟라이트(커맨드 + 스페이스)에서 "터미널"을 검색하면 됩니다.
윈도우에서는 윈도우 키를 누르고 "cmd" 또는 "powershell"을 입력합니다.

**[화면: 노드 버전 확인]**

```bash
node -v
```

이 명령어를 입력하고 엔터를 누릅니다.
버전 숫자가 표시되면 이미 설치되어 있는 것입니다.

18 이상이면 정상입니다. 예를 들어 v20.11.0, v22.4.0 모두 가능합니다.

버전이 18 미만이거나 "command not found"가 나오면 노드제이에스 공식 사이트에서 설치해야 합니다.

**[화면: nodejs.org 접속]**

nodejs.org에 접속하면 엘티에스(LTS) 버전 설치 버튼이 있습니다.
엘티에스는 Long-Term Support의 약자인데요, 안정적인 장기 지원 버전입니다.
이 버튼을 클릭해서 설치 파일을 내려받고 설치합니다.

설치 후 터미널을 새로 열고 `node -v`를 다시 입력해서 확인합니다.

---

## 2단계 — 앤스로픽 에이피아이 키 발급 (5분)

**[화면: console.anthropic.com 접속]**

브라우저에서 console.anthropic.com에 접속합니다.

계정이 없으면 회원가입을 먼저 합니다.
이메일과 비밀번호로 가입하면 됩니다.

**[화면: API Keys 메뉴]**

로그인 후 왼쪽 메뉴에서 "API Keys"를 클릭합니다.
"Create Key" 버튼을 눌러 키를 생성합니다.

키 이름은 "lecture" 또는 본인이 구분하기 쉬운 이름으로 입력합니다.

**중요**: 생성된 키는 이 화면에서만 전체를 볼 수 있습니다.
화면을 닫기 전에 반드시 복사해서 안전한 곳에 저장합니다.

---

## 3단계 — 클로드 코드 설치 (5분)

**[화면: 터미널]**

앤스로픽 공식 설치 방법입니다.

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

이 명령어 하나로 설치가 완료됩니다.

설치가 끝나면 확인합니다.

```bash
claude --version
```

버전 번호가 표시되면 설치가 정상입니다.

---

## 4단계 — 에이피아이 키 등록 (3분)

**[화면: 터미널]**

클로드 코드가 에이피아이 키를 사용하려면 환경 변수로 등록해야 합니다.

맥 기준입니다.

```bash
echo 'export ANTHROPIC_API_KEY="여기에-키-입력"' >> ~/.zshrc
source ~/.zshrc
```

따옴표 안에 아까 복사한 키를 붙여넣습니다.

윈도우 파워쉘 기준입니다.

```powershell
$env:ANTHROPIC_API_KEY="여기에-키-입력"
```

영구 등록이 필요하면 시스템 환경 변수에 추가합니다.

---

## 5단계 — 첫 실행 확인 (2분)

**[화면: 터미널]**

```bash
mkdir test-session
cd test-session
claude
```

프롬프트가 표시되면 정상 실행된 것입니다.

다음을 입력합니다.

```
이 폴더에 hello.txt 파일 만들어줘. 내용은 "클로드 코드 시작"
```

클로드 코드가 어떤 도구를 사용하는지, 어떤 파일을 생성하는지 터미널에 표시됩니다.
결과를 보여주는 것이 아니라 과정이 투명하게 보이는 것이 클로드 코드의 특징입니다.

세션을 종료할 때는 `/exit`를 입력하거나 컨트롤+디를 누릅니다.

---

## 마무리 (1분)

설치와 첫 실행을 완료했습니다.

노드제이에스 설치, 에이피아이 키 발급, 클로드 코드 설치, 첫 실행 확인까지 모두 마쳤습니다.

이제 CH01로 넘어갑니다.
CH01에서는 클로드 코드가 어떤 구조로 동작하는지, 에이전트가 무엇인지부터 시작합니다.

---

## 🗒️ 서연 메모
- [ ] 터미널 폰트 크기 키우기 (화면 녹화 전, 18pt 이상)
- [ ] 맥/윈도우 각각 화면 분리 녹화 또는 자막 처리
- [ ] 앤스로픽 콘솔 화면: 실제 키 생성 과정 시연 (키 일부 블러 처리)
- [ ] 첫 실행 시 hello.txt 생성 결과 확인 화면 캡처
