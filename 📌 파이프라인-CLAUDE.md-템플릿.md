# 📌 데이터 분석 파이프라인 CLAUDE.md 템플릿

> CH09~CH14 프로젝트 챕터에서 공통으로 사용하는 템플릿.
> 각 프로젝트 시작 시 이 내용을 CLAUDE.md에 붙여넣고, ★ 표시 항목만 수정합니다.

---

```markdown
# [★ 프로젝트명] 데이터 분석 프로젝트

## 프로젝트 개요
★ 분석 목적 한 줄:
★ 타겟 수강생 직무:
★ 핵심 질문 3가지:
  1.
  2.
  3.

## 폴더 구조 규칙
- data/raw/       ← 원본 데이터 (절대 수정 금지)
- data/processed/ ← 전처리 완료 데이터
- data/output/    ← 분석 결과 수치
- charts/         ← 시각화 파일 (.png, .html)
- reports/        ← 최종 보고서 (.md)
- scripts/        ← 파이썬 스크립트

---

## 🔄 8단계 데이터 분석 파이프라인

새 데이터 분석 요청 시 반드시 아래 순서를 따릅니다.

### ① 문제 정의 (Problem Definition)
- 분석 목적을 한 문장으로 명시합니다
- 핵심 질문 3개를 기준으로 분석 범위를 결정합니다
- ★ 이 프로젝트의 고정 문제 정의:

### ② 데이터 전처리 (Preprocessing)
- data/raw/ 파일 읽기 → 구조 파악 (수정 금지)
- 결측값 처리:
  - 수치 컬럼: 중앙값 또는 평균으로 대체
  - 카테고리 컬럼: 'Unknown' 또는 최빈값으로 대체
- 날짜 형식: YYYY-MM-DD 통일
- 이상치: IQR × 1.5 기준 탐지 후 별도 표시 (삭제 금지)
- 중복 행: 제거
- 결과 저장: data/processed/cleaned_data.csv

### ③ 지표 설계 (Metric Design)
★ 이 프로젝트의 핵심 KPI와 계산 공식:
- KPI 1:
- KPI 2:
- KPI 3:

공통 지표:
- 증감률 = (현재값 - 이전값) / 이전값 × 100
- 비중 = 항목값 / 전체합 × 100

### ④ 방법론 (Methodology)
★ 이 프로젝트에서 사용하는 분석 기법:
- [ ] 기술 통계 (평균, 중앙값, 표준편차)
- [ ] 시계열 분석 (트렌드, 계절성)
- [ ] 그룹별 집계 (피벗, 코호트)
- [ ] 상관관계 분석
- [ ] 에이비 테스트 결과 해석

### ⑤ 인사이트 도출 (Insights)
- 발견한 패턴을 사실 기반 문장으로 서술합니다
- 형식: "★ 지표가 ★ 수준이며, ★ 원인으로 추정됩니다"
- 인사이트 최소 3개, 최대 7개
- 각 인사이트에 근거 수치 반드시 포함
- 저장: data/output/insights.md

### ⑥ 데이터 시각화 (Visualization)
차트 유형 선택 기준:
- 시간 데이터 → 라인 차트
- 카테고리 비교 (7개 이하) → 세로 막대
- 카테고리 비교 (8개 이상) → 가로 막대
- 비율/구성 → 파이 차트 (5개 이하만)
- 두 변수 관계 → 스캐터 플롯
- 그룹별 분포 비교 → 박스 플롯

색상 기준:
- 강조 (핵심 지표): #2563EB (진한 파랑)
- 일반: #9CA3AF (연한 회색)
- 긍정/상승: #059669 (초록)
- 경고/하락: #DC2626 (빨강)

차트 공통 설정:
- 크기: 10×6 인치, 150dpi
- 제목: 기술적 설명 금지, 인사이트 한 줄로
- 저장: charts/ 폴더

★ 이 프로젝트 필수 차트:
- 차트 1:
- 차트 2:
- 차트 3:

### ⑦ 대시보드 생성 (Dashboard)
- 플롯리(Plotly)로 인터랙티브 대시보드 생성
- 구성:
  - 상단 KPI 카드: 핵심 지표 3~5개 (숫자 + 증감률)
  - 중단 주요 차트: 2~3개
  - 하단 상세 테이블: 필요 시 추가
- 필터: 날짜 범위 또는 카테고리 드롭다운
- 저장: charts/dashboard.html

### ⑧ 보고서 생성 (Report)
- 마크다운으로 분석 보고서 작성
- 구성:
  1. 분석 배경 및 목적
  2. 데이터 개요 (행수, 기간, 주요 컬럼)
  3. 방법론 요약
  4. 주요 결과 (인사이트 + 차트 참조)
  5. 결론 및 권고사항
  6. 다음 단계 제안
- 저장: reports/analysis_report.md

---

## 시각화 색상 기준
(⑥ 항목 색상 기준과 동일)

## 출력 형식
- 수치: 소수점 둘째 자리까지
- 금액: 원화, 천 단위 콤마 (예: 1,500,000원)
- 비율: 퍼센트 기호 포함 (예: 23.5%)
- 날짜: YYYY-MM-DD 형식

## 금지 사항
- data/raw/ 파일 수정 금지
- 인사이트 없이 차트만 생성 금지 (반드시 ⑤ 먼저)
- 보고서 없이 분석 완료 선언 금지
```

---

## 챕터별 ★ 수정 항목 가이드

| 챕터 | 프로젝트명 | 핵심 KPI | 분석 기법 | 필수 차트 |
|------|-----------|---------|----------|---------|
| CH09 | 마케팅 대시보드 | 알오아이, 전환율, 채널별 지출 | 채널별 집계, 시계열 | 채널별 알오아이 막대, 월별 트렌드 라인, 예산 파이 |
| CH10 | 에이비 테스트 | 전환율 차이, 통계적 유의성, 상승폭 | 에이비 테스트 해석, 신뢰구간 | 전환율 비교 막대, 신뢰구간 차트, 누적 전환 라인 |
| CH11 | PM KPI | 디에이유, 리텐션, 피쳐 사용률 | 코호트 분석, 시계열 | 디에이유 트렌드, 코호트 히트맵, 피쳐별 사용률 막대 |
| CH12 | 리텐션·매출 | 코호트 리텐션율, 엘티브이, 매출 트렌드 | 코호트 분석, 집계 | 코호트 히트맵, 매출 라인, 리텐션 곡선 |
| CH13 | HR 자동화 | 이직률, 근속 기간, 팀별 성과 | 기술통계, 그룹 집계 | 팀별 이직률 막대, 근속 기간 박스플롯, 성과 스캐터 |
| CH14 | 오픈클로 파이프라인 | 실행 횟수, 성공률, 처리 시간 | 시계열, 집계 | 일별 실행 라인, 성공률 막대, 처리 시간 분포 |
