# 강의 스크립트 작성 규칙

이 폴더의 모든 클립 대본을 일관된 스타일로 작성합니다.

---

## 파일 형식

### 프론트매터
```yaml
---
tags: #파트 #챕터 #키워드
time: 10분
part: Part 02
chapter: CH02
clip: 01
slides: s2,s3,s14,s4,s16
status: ✅ 대본완성
---
```

### 섹션 헤더
슬라이드 ID를 포함해서 작성합니다.
```
## s{ID} — 슬라이드 제목
```
섹션 브레이크 슬라이드:
```
## s{ID} — CLIP {번호} 섹션 브레이크
```

---

## 말투 규칙

- **"요"와 "습니다" 혼용** — 단순 서술은 "있습니다/했습니다", 설명·연결은 "이에요/건데요", 전환·예고는 "해보도록 하겠습니다/볼게요"
- 자연스러운 구어체 — 번역체 금지, 강의 현장에서 말하듯. 한 단락 안에서도 어미를 섞어서 리듬감 있게
- 문장은 짧게, 한 문장에 하나의 내용만
- 화면 지시는 `**[화면: ...]**` 마커 사용
- 강조 단어는 **볼드** 처리

---

## 내용 규칙

- 각 섹션은 해당 슬라이드에 실제로 보이는 내용만 설명
- 슬라이드 순서 = 스크립트 섹션 순서
- 코드나 명령어는 코드블록으로
- 섹션 브레이크 슬라이드는 클립 주제를 2~3줄로 도입
- 마지막 섹션은 다음 클립 예고로 마무리
- 슬라이드에 없는 내용 추가 금지
- 과도한 부연 설명 금지

---

## 슬라이드 기획 판단 규칙

슬라이드를 새로 만들거나 기존 슬라이드를 검토할 때 아래 질문을 먼저 거친다.

1. **이미 배운 내용인가?** — 같은 개념을 이전 파트·챕터에서 이미 다뤘다면 슬라이드로 만들지 않는다. 반복 설명은 수강생 시간 낭비.
2. **이 슬라이드가 없으면 흐름이 끊기는가?** — 없어도 자연스럽게 이어지면 삭제가 정답.
3. **새로 가르치는 게 있는가?** — 기존 개념의 "적용 예시" 또는 "이 챕터 고유 내용"이 있어야 존재 이유가 생긴다.

판단 후 삭제가 맞으면 ORDER 배열에서 제거한다. HTML 블록은 남겨둬도 무방.

---

## 코드·터미널 블록 레이아웃 규칙

슬라이드 안에 dark 터미널 박스(폴더 트리, 코드 등)를 넣을 때 콘텐츠가 잘리지 않도록 아래를 지킨다.

- 부모 컨테이너는 `align-items:flex-start` — `center`로 두면 콘텐츠가 위아래로 넘쳐서 잘림
- 바깥 여백: `padding:20px 28px` 이하로 절제 (32px 이상 금지)
- 터미널 박스 내부 패딩: `padding:20px 24px`
- `font-size:15px`, `line-height:1.85` — 2.0 이상은 줄 수가 많을 때 넘침 발생
- 슬라이드 제작 후 반드시 브라우저에서 확인 — 잘림 여부는 눈으로만 검증 가능

---

## 슬라이드 레이아웃 — 개별 컨테이너 원칙

슬라이드 안에 여러 항목을 나열할 때 **전체 배경·구분선 방식 금지**.
각 항목은 반드시 테두리가 있는 **개별 카드 컨테이너**로 분리한다.

**금지 패턴:**
- 컬럼에 `border-right`만 쓰고 배경색으로 구분
- 전체 영역에 배경색 깔고 내부 텍스트만 나열

**올바른 패턴:**
```html
<!-- 각 항목 = 독립 카드 -->
<div style="border:1px solid var(--border);border-radius:8px;padding:24px 20px;">
  ...
</div>
```

- 강조 항목은 `border-color`를 `rgba(232,96,60,0.35)` + `background:rgba(232,96,60,0.03)` 조합
- 카드 사이 간격: `gap:10px~14px`
- 플로우 연결 화살표: `→` 텍스트, `color:#CCC`, `flex-shrink:0`

이 규칙은 커버 슬라이드의 클립 카드, 원칙 나열, 단계별 흐름 등 **모든 다중 항목 나열에 적용**.

---

## 비유·예시

- 어려운 개념은 일상 비유로 먼저 풀어주고 기술 용어 사용
- 슬라이드에 비유가 있으면 그 비유 그대로 설명
- 예시는 슬라이드에 나오는 것 그대로

---

## 슬라이드 헤드라인 말투 규칙

슬라이드 제목(big-title, sec-title, 섹션 주요 헤드라인)에 적용하는 어미 규칙.

### 핵심 원칙: 선언문 금지, 명사형/~기형으로 끝낸다

| 나쁜 예 (금지) | 좋은 예 |
|---|---|
| 코딩 전에 먼저, 경험을 설계한다 | 차트 자체보다는, 경험을 설계하기 |
| 데이터를 시각화한다 | 데이터를 시각화하기 |
| 이렇게 접근한다 | 이렇게 접근하기 |
| 레퍼런스를 먼저 찾는다 | 레퍼런스를 먼저 찾기 |

- `~다` / `~이다` / `~했다` / `~됩니다` / `~합니다` — 모두 금지
- `~기` (동명사형) 또는 명사로 끝낼 것
- 명사만으로도 충분하면 동사 자체를 쓰지 않음 (예: "경험 설계", "인코딩 선택")

### 2줄 헤드라인 구조 (권장)

```
1줄: 대비·맥락 (조사로 끝, 예: ~보다는, ~에서, ~일 때)
2줄: 핵심 개념 (명사 또는 ~기형)
```

예시:
```
차트 자체보다는,
경험을 설계하기
```

### 슬라이드 1장 = 메시지 1개

- 헤드라인이 메시지여야 함 — 슬라이드 제목이 토픽이 아니라 결론
- 슬라이드만 봐도 무슨 말인지 이해가 가야 함
- 설명형·나열형보다 대비·흐름 다이어그램으로 시각화

---

## 섹션 구분

섹션 사이는 `---` 구분선 사용
