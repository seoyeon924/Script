---
tags: #CH07 #D3.js #인터랙티브 #필터 #애니메이션 #줌
time: 20분
chapter: CH07
status: ✅ 대본완성
---

# CH07-02 인터랙티브 기능 — 필터, 애니메이션, 줌

## 📌 클립 정보
- **예상시간:** 20분
- **유형:** 화면 녹화 위주
- **준비물:** 클로드 코드, 브라우저

---

## 학습 목표 선언 (30초)

이 클립을 마치면 두 가지를 할 수 있습니다.

첫 번째, 클로드 코드에게 필터 버튼, 애니메이션, 줌 기능을 추가하도록 정확히 지시할 수 있습니다.
두 번째, 정적 차트를 인터랙티브 대시보드 수준으로 업그레이드하는 흐름을 설계할 수 있습니다.

---

## 인터랙티브 기능 세 가지 (3분)

디쓰리제이에스가 다른 라이브러리보다 강력한 이유가 인터랙티브 기능입니다.

세 가지 기능을 다룹니다.

**첫 번째, 필터 버튼입니다.**
버튼을 클릭하면 특정 카테고리만 차트에 표시합니다.
"전체 / SNS만 / 검색만" 같은 버튼이 있는 방식입니다.

**두 번째, 애니메이션입니다.**
차트가 처음 로드될 때 막대가 서서히 올라오거나, 선이 그려지는 효과입니다.
데이터가 바뀔 때 부드럽게 전환하는 트랜지션도 포함됩니다.

**세 번째, 줌과 팬입니다.**
데이터 포인트가 많을 때 마우스 스크롤로 확대하고 드래그로 이동합니다.
시계열 데이터에서 특정 구간을 자세히 볼 때 유용합니다.

---

## 필터 버튼 추가 지시 방법 (5분)

이전 클립에서 만든 막대 차트에 필터를 추가합니다.

> "charts/d3_bar.html을 열어서 필터 버튼을 추가해줘. 차트 상단에 버튼을 만들어줘. 버튼 목록: '전체', 'SNS', '검색', '이메일', '디스플레이'. 버튼을 클릭하면 해당 채널 막대만 표시하고 나머지는 사라지게 해줘. 현재 선택된 버튼은 파랑으로 강조 표시해줘."

에이전트가 기존 코드를 분석하고 필터 기능을 추가합니다.

---

## 애니메이션 추가 지시 방법 (5분)

차트 로드 시 막대가 올라오는 애니메이션을 추가합니다.

> "charts/d3_bar.html에 두 가지 애니메이션을 추가해줘. 첫 번째, 페이지 로드 시 막대가 아래에서 위로 0.8초 동안 올라오는 애니메이션. ease-out 방식으로. 두 번째, 필터 버튼 클릭 시 사라지는 막대는 0.3초 동안 페이드 아웃, 나타나는 막대는 0.5초 동안 올라오는 애니메이션."

디쓰리제이에스의 `transition()` 함수로 처리합니다.
클로드 코드가 정확한 코드를 작성합니다.

---

## 줌 기능 추가 지시 방법 (4분)

시계열 라인 차트에 줌과 팬을 추가합니다.

> "새 파일 charts/d3_zoom.html을 만들어줘. data/monthly.csv를 읽어서 월별 매출 라인 차트를 그리고, 마우스 스크롤로 확대·축소, 드래그로 이동하는 줌 기능을 추가해줘. 줌 범위: 1배에서 10배까지. 줌 아웃 버튼도 우측 상단에 추가해줘."

---

## 직접 해보기

**목표**: 필터 + 애니메이션이 있는 인터랙티브 차트를 만듭니다.

**준비 (터미널에 복붙)**
```bash
cd ~/d3-test
claude
```

**클로드에게 이렇게 말합니다**
```
charts/d3_bar.html에 두 가지 기능을 추가해줘.

1. 상단 필터 버튼: 전체 / SNS / 검색 / 이메일 / 디스플레이
   - 클릭하면 해당 채널만 표시
   - 선택된 버튼은 #2563EB 파랑으로 강조

2. 애니메이션:
   - 페이지 로드 시 막대가 0.8초 동안 아래에서 올라옴
   - 필터 전환 시 0.4초 트랜지션
```

**결과 확인**
```bash
open charts/d3_bar.html
```

→ 버튼 클릭하면 막대가 부드럽게 필터링됩니다.

---

## 핵심 정리 (30초)

필터는 버튼 클릭 이벤트와 데이터 업데이트로 구현합니다.
애니메이션은 `transition().duration()` 으로 처리합니다.
줌은 디쓰리제이에스의 `zoom()` 함수가 담당합니다.
클로드 코드에게 기능을 명시하면 코드를 작성합니다.

## 다음 클립 예고 (30초)

다음 클립에서는 데이터 분석에 특화된 커스텀 차트를 다룹니다.
히트맵, 트리맵, 네트워크 그래프처럼 일반 라이브러리로는 만들기 어려운 차트를 자동 생성합니다.
